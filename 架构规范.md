# 《玄穹问道》代码架构规范

## 1. 固定依赖方向

新功能必须遵守以下单向依赖：

```text
scenes / ui  →  domain  →  core 数据仓库
```

- `scenes/`：Phaser 生命周期、资源加载、镜头和各模块装配。
- `ui/`：绘制、动画、点击反馈；不能直接实现交易、配装或战斗规则。
- `domain/`：与画面无关的游戏规则，可在 Node 中独立测试。
- `core/`：存档、模板和基础配置；不能导入场景或 UI。

禁止 UI 模块调用另一个页面的业务方法。例如背包和功法不能通过
`VillageScene.getMerchantItems()` 读取物品，三者应共同依赖 `ItemCatalog`。

代码规模不是架构边界。文件可以超过 1186 行，不设置强制行数上限；是否合理取决于
职责是否单一、结构是否清楚、依赖是否正确、规则能否独立验证。拆分是为了解决耦合，
不是为了追求较小的行数。

## 2. 当前领域边界

### 物品目录 `domain/items`

负责物品模板查询、按 ID 查询和角色持有物品查询。不负责价格结算、商人库存、
功法槽位或 UI 纹理绘制。运行时纹理由场景通过 `resolveTexture` 适配。

### 背包规则 `domain/inventory`

负责物品数量、使用、丢弃、成功率、属性恢复、临时加成与到期回滚。UI 只能展示
`InventoryService` 返回的结果；计时器可以由 Phaser 页面调度，但数值回滚必须由领域服务完成。

### 功法 `domain/techniques`

负责主修、四个辅修和速度位的结构、拥有校验、槽位规则、去重装备与卸下。
功法页面只能调用 `TechniqueLoadoutService`，不能直接修改
`gameState.player.equippedTechniques`。

功法画面位于 `ui/techniques/TechniquePanel.js`，不得回写角色菜单外壳。

### 法术 `domain/spells`

负责先天五行法术和已掌握法术的查询。法术是战斗中可施放的能力；功法可以提供
法术，但“法术页面”和“功法配装页面”不是同一个模块。

法术画面位于 `ui/spells/SpellPanel.js`，只能消费 `SpellService` 返回的普通数据。
页面不得读取 `equippedTechniques`、`learnedTechniques`，也不得通过功法页面推导法术。

### 商店 `domain/shop`

负责商人库存、购物总价以及购买/出售事务。一次交易必须同时校验并更新玩家货币、
商人货币、商人库存和玩家背包，不能由 UI 分多步直接修改。

商店界面的生命周期、页面控件和购物会话状态均由 `ui/merchant/MerchantPanel.js`
唯一持有。商品区、购物车、数量输入、确认弹窗以及它们的输入命中均属于该面板。
地图场景只负责装配服务、打开面板和转发全局输入；禁止在场景新增 `merchantMode`、
`merchantCart` 等镜像字段或商店页面兼容方法，也不得绕过 `MerchantPanel`。

### 法宝 `ui/artifacts`

法宝页拥有独立 `ArtifactPanel`，六类槽位、拥有校验、类别限制、装备去重和卸下由
`domain/artifacts/ArtifactLoadoutService` 负责。页面只能调用该服务，禁止读取
`equippedArtifacts` 或直接修改背包。未来炼化、耐久等规则也必须先进入法宝领域。

### 角色菜单 `ui/character`

`CharacterMenuPanel` 是属性、储物袋、法宝、法术、功法等角色页的公共外壳，只负责
全屏生命周期、一级导航和子页调度。`StorageBagPanel` 是纯背包子页，不得创建公共
背景、持有 `activeTab`，也不得实例化法宝或功法页面。子页之间禁止互相调用。

### 战斗 `domain/combat`

`CombatEngine` 负责先手、动作锁、随机伤害、灵气消耗、防御减伤、敌方技能选择、
冷却、回合推进和胜负。`BattleScene` 只播放动画、音效并显示引擎结果；不得直接扣除
双方生命或灵气，也不得使用 Phaser 随机 API 决定战斗数值。

### 战斗奖励 `domain/rewards`

`BattleRewardService` 负责解析怪物掉落、灵石与修炼经验入账、物品入包、怪物击败标记、
章节精英推进和重复结算保护。`BattleScene` 只能展示服务返回的实际结算结果，不能直接修改
`spiritStones`、`cultivationExp`、`inventory`、`defeatedMonsterIds` 或 `eliteDefeated`。

怪物编辑器暂时保留“名称 × 数量”文本格式；物品名称必须存在于统一物品目录。未登记名称
不得伪装成已获得，结算结果必须明确显示“未登记，未入包”。测试战斗不发奖励、不推进任务。

`RewardCatalog` 是系统奖励与物品奖励的统一可选目录。怪物编辑器必须通过
`ui/editor/MonsterDropEditorPanel` 选择目录项和数量，禁止重新提供自由文本掉落输入。
存储层暂时继续使用“名称 × 数量”字符串以兼容旧模板；旧版未登记文本可显示、保留或删除，
但不能从新编辑器中新增。

### 世界探索 `domain/world`

`MapExplorationService` 负责大地图足迹的记录间距、坐标规范、旧档修复和容量上限，并且是
`world.miniMapVisitedPoints` 的唯一运行时写入入口。`ChapterMapHud` 只能取得坐标快照并绘制
“观山镜”，不得直接导入 `gameState`、向足迹数组追加内容或在 UI 中重新定义采样规则。

足迹更新仍跟随统一自动存档节奏，不能在每次角色移动时频繁写入浏览器存储。将来增加多地图探索时，
应先扩展领域服务与存档迁移，再让 HUD 消费新的普通数据，不得在页面中临时增加地图分支。

### 存档与迁移 `core/save`

`SaveMigrations` 是角色存档结构、默认值、当前 schema 版本和迁移注册表的唯一来源。
`GameState` 只负责浏览器档案位读写和把迁移后的数据装入运行时状态，不得在加载函数中散落
旧档补字段逻辑，也不得自行写死版本号。

存档升级必须逐版注册 `vN → vN+1`，迁移不得修改传入对象，当前版本重复迁移必须幂等。
高于当前支持版本的存档必须拒绝读取，禁止旧代码降级覆盖。五档位容器和单个角色存档分别
维护版本；本地数据备份的 `version` 只描述备份文件格式，不代表角色存档 schema。
稳定键解析失败或版本过新后，当前运行必须锁定存档写入；即使界面取得空档位，也不能覆盖
无法理解的原始数据。

浏览器当前档案位使用稳定键 `xuanqiong-wendao-save-slots`。历史 `-v1` 档案只读兼容，
成功迁移后复制到稳定键，不主动删除旧键，以便异常时仍可人工恢复。

## 3. 新增功能规则

1. 先建立领域服务和可独立测试的规则，再接 Phaser UI。
2. 查询方法原则上不修改数据；必须迁移或初始化时使用明确命名的方法。
3. 跨模块只传 ID 和普通数据对象，不传 Phaser DisplayObject。
4. 场景可以包含较多 Phaser 表现代码，但商店交易、任务规则等独立业务应放入领域服务。
5. 存档兼容集中在加载/迁移层，不能在页面 `render()` 中修补旧档。
6. 每个领域至少验证成功、非法输入、旧档和存取一致四类路径。

## 4. 后续拆分顺序

1. 把章节任务状态流迁入独立任务领域服务。
2. 将怪物掉落持久化格式从名称文本升级为稳定奖励 ID，并提供模板迁移。
