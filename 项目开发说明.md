# 《玄穹问道》项目开发说明

> 给后续开发者（包括另一台电脑上的 Codex）阅读。
>
> 这是一个长期开发的单人离线修仙 RPG。开始任何功能开发前，请先阅读本文，再查看 `src/` 中对应模块的中文注释。

## 0. 换电脑接手时先做这些（最高优先级，更新于 2026-08-28）

> 本节是当前最高优先级交接入口。后文较早日期中的路径和开发顺序属于历史记录；如有冲突，
> 以本节、最后一章第 38 节和 `架构规范.md` 为准。

1. 复制**整个项目目录**到另一台电脑，不能只复制 `src`。必须包含 `.git`、`public`、
   `scripts`、`AGENTS.md`、`项目开发说明.md`、`架构规范.md`、`package.json` 和全部未跟踪文件。
2. 本项目当前有尚未提交的角色创建领域化改动。复制后先运行 `git status --short`，禁止使用
   `git reset --hard`、`git checkout -- .` 或类似命令清掉改动。
3. 浏览器角色档案仍不会随源码目录自动迁移，换机前要通过游戏设置重新导出最新角色资料；控制台的
   物品、怪物、NPC、建筑、地图摆放和上传图片已经改为项目文件，复制整个项目目录即可带走。历史
   `玄穹问道-数据备份-2026-08-24_23-22-12.json` 当前在工作区中显示为删除，不能再把它当作可用备份。
4. 新电脑上的 Codex 必须先完整阅读 `AGENTS.md`、本文和 `架构规范.md`，然后才能修改代码。
   **是的，必须明确让另一台电脑读取 `架构规范.md`；仅阅读本文还不够。**
5. 在项目根目录先运行 `npm test`。2026-08-28 收工时的正确基线是 1 项架构检查和 28 组冒烟测试全部
   通过，完整说明见第 38.6 节。若复制后基线失败，应先查明文件是否漏拷，不要直接改业务代码掩盖问题。
6. 启动游戏优先使用 `启动游戏.bat` 或 `node server.cjs`，再访问 `http://localhost:8000`。
   不要直接双击 `index.html`。某些 Windows 电脑的 `python.exe` 只是商店占位符，因此不把
   `python -m http.server` 作为唯一启动方式。
7. 2026-08-28 已完成修为封顶、突破小游戏、第一章冒险循环、古玉战斗卡死修复和角色创建领域化。
   下一次开发必须按最后一章第 38 节接手；第 13.8、15.4、27.8 节中的旧顺序只作历史记录。

给另一台电脑 Codex 的推荐开场指令：

```text
请先完整阅读项目根目录的 AGENTS.md、项目开发说明.md、架构规范.md，三份都不能跳过；
再运行 git status --short 和 npm test。保留当前全部未提交改动，不要重置工作区。
请先阅读项目开发说明最后一章第 38 节的当日交接和推荐后续顺序，再继续开发，并严格遵守架构边界。
代码可以超过 1186 行，不设强制行数上限；是否拆分只按职责、依赖和可测试性判断。
```

## 1. 项目目标

- 游戏名称：**玄穹问道**。
- 类型：俯视角、自由探索、剧情与养成并重的修仙角色扮演 RPG。
- 参考感觉：从上往下看地图，角色能上下左右自由移动；遇怪切换到独立的回合制战斗界面。
- 美术方向：仙侠、水墨、国风；地图和战斗使用用户提供的原创素材。
- 运行方式：HTML5 网页游戏，可直接在浏览器运行。
- 固定技术栈：**HTML + JavaScript + Phaser.js**。
- 游戏模式：单人、离线、本地存档。
- 语言：简体中文。
- 屏幕设计基准：**1920×1080（16:9）**。所有界面直接使用 Full HD 一对一坐标和尺寸；例如代码写 `115×115`，屏幕就显示 `115×115`，不再使用 1.5 倍镜头缩放。

## 2. 开发原则（非常重要）

1. 用户是编程零基础。每次开发都必须先用通俗中文说明模块设计，再写带详细中文注释的代码，最后说明如何使用和修改。
2. 项目定位为中大型游戏。不能把所有功能堆到一个场景或一个文件里；数据、场景、战斗、编辑器必须分模块。
3. 地图、NPC、怪物、物品、法术等内容要**数据驱动**：先建模板，再由地图引用模板。修改模板后，引用它的内容应同步更新。
4. 优先实现“可运行、可测试”的最小版本，再逐步丰富内容、美术、数值与编辑器。
5. 不能随意删除用户已有素材、存档或不相关代码。

## 3. 已确认的核心玩法

### 3.1 探索与地图

- 主世界是俯视角地图，键盘 WASD / 方向键自由移动。
- 鼠标点击可自动前往；当前设计为**不会主动绕开怪物**。
- 大区域之间切换，小区域内部连续探索。
- 第一章区域：**青云山**，出生地为**栖霞村**。
- 青云山是用户提供的 5×5 大地图拼图，已实现按需加载，方便未来继续增加新的大地图。
- 角色可通过御剑、飞舟等后期方式跨越地形；步行不能穿过山体、树木等阻挡物。飞舟仅能带 5 人，有载货容量。
- 地图编辑器需要支持中键拖动平移、滚轮缩放、全地图查看、放置内容并保存。

### 3.2 角色创建与存档

- 游戏封面有：踏入仙途、设置、开发者控制台三个入口。
- 点击踏入仙途后进入角色档案选择，最多 5 个角色档案，可创建、进入、删除。
- 创建角色时可以设置：名字、性别、外观（外观系统后续继续完善）。
- 创建角色时有 10 点五行灵根潜能，可自由分配给金、木、水、火、土。
- 一种属性可全部加点；单一高属性有更强的专精/大成效果。
- 浏览器本地存档，重大剧情、进秘境、突破前等后续要有自动存档。最多 10 个手动存档位是长期目标。
- 允许读取旧档重来。

### 3.3 修炼、属性与境界

- 主角初始只有：金、木、水、火、土五灵根。
- 风、雷、冰、魔、神为后期特殊属性，只能通过奇遇、血脉、功法、法宝等获得。
- 功法/术法只能学习符合自身相关属性的内容。
- 每项属性最高通常为 99；特殊的“十大金丹”等极稀有系统可突破到 105，可多次使用。
- 每个小境界（初期、中期、后期、大圆满）提升会获得成长点；野外高难材料也能加点。
- 经典境界路线：炼气 → 筑基 → 金丹 → 元婴 → 化神……后续继续扩展飞升。
- 不显示传统角色等级数字，主要显示境界与初/中/后/大圆满。
- 突破大境界必须有操作小游戏和失败后果：低境界可掉修为，高境界极低概率死亡；稀有丹药可把成功率提高最多 50 个百分点，例如 20% 提高至最多 70%。
- 可以闭关；可强制中断闭关，可能有代价。

### 3.4 回合制战斗

- 遇怪后进入独立战斗画面，双方轮流行动。
- 每回合只能选择一个行动：施放一个术法、使用物品、防御或逃跑。
- 当前快捷栏目标为 1～9 自由摆放；初版中 1～3 已放入普通攻击、术法、防御。
- 强力技能可以有回合冷却；灵气不足时只能普通攻击，防御可恢复少量灵气。
- 玩家队伍最多 5 个战斗单位：主角 + 最多 3 名 NPC 同伴 + 1 只灵兽；没有灵兽时可带 4 名 NPC。
- 未来门派大战可用九宫格站位，每方 3×3；普通小队最多 5 人，重要门派战争可多批次出战。
- 战斗结束可选择灭杀或放过敌人，影响善恶、阵营、材料等。

### 3.5 五行和战斗属性

- 属性：金、木、水、火、土、风、雷、冰、魔、神。
- 必须有属性克制关系，具体数值和完整克制表后续设计并平衡。
- 角色、妖、魔等每项属性通常不超过 99。
- 技能会消耗灵气；功法、阵法、符箓、丹药、剑术和法术能自由组合。
- 每个 NPC 都有不同灵根属性，也可学习功法、术法；主角/角色同时有主修功法、辅助功法、特殊秘术的长期设计。

### 3.6 世界、剧情与势力

- 开局：偏远小村的普通人，通过偶得机缘（残缺功法、古玉或神秘法宝）接触修仙世界。
- 第一章主题：栖霞村的古玉；古玉是上古飞升者留下的遗物。
- 结构：前期有简短引导，之后完全自由；主线完整，同时可自由刷资源、做任务、培养角色。
- 剧情为自由多分支。选择会改变阵营、伙伴关系和结局；重要对话会影响好感和主线。
- 游戏有日期、季节、年龄、寿命；现实时间会推进游戏时间，但需要暂停功能。修仙者寿命应远高于普通人，避免过早死亡。
- 主角可加入一个门派，后期也可自建门派；可结盟、灭门、赔偿修复关系、通过特殊任务改善关系。
- 门派最多 100 人，分三流、二流、一流。正魔两道在不同区域各有强大门派。
- 善恶/声望范围：-100（极恶魔道）到 +100（大善正道）。伤害普通村民会导致正道、商人、城市等作出反应。
- 正道善值高时正道功法伤害更强；善高学习魔功伤害较低。极稀有永久丹药可解除这类限制，可重复服用。

### 3.7 灵兽、同伴与城市（长期系统）

- 灵兽栏最多 10 只，出战 1 只；多余灵兽可存放灵兽园/门派。
- 灵兽有捕捉小游戏，可能逃跑或呼唤同类；高阶灵兽能进化/血脉觉醒。
- 部分怪物有巢穴，可以收为灵兽；不是所有怪物都能捕捉。
- NPC 可通过任务、随机事件加入；可能离队、背叛、永久死亡，也可能通过稀有道具或特殊任务救回。
- 城市与村庄可被毁，人会死亡，但长期会有人迁入重建。城市有管理者，可由玩家任命 NPC；城市最多三个等级，人口上限由等级决定。

### 3.8 装备、炼制、经济和特殊内容（长期系统）

- 装备没有耐久度，只有强化；高强化失败可能损坏，需特殊材料修复。
- 需要颜色品质等级体系，具体颜色和阶级由后续统一设计。
- 炼丹、炼器有熟练度与等级要求；所有丹药都必须有丹方。丹方来源：门派、城市、商人、精英怪等。
- 炼丹、炼器和突破都不能只是加载条，要有操作小游戏和失败结果。
- 灵石是人界主货币；魔界、妖界要使用不同货币（后续设计）。
- 十大神器：开局就存在于世界，图鉴中可见基础介绍；击败守护者可直接获得；神器固定属性、只能装备一件。不能制作十大神器。
- 十大金丹：需收集材料和丹方炼制，用于突破属性上限。
- 不做烹饪系统。

## 4. 开发者控制台与编辑器架构

### 4.1 总原则

控制台是全部内容工具的总入口，不是单独的地图编辑器。编辑器必须把数据保存到浏览器本地；游戏进入地图/战斗时读取最新数据，保存后立即生效。

### 4.2 编辑器清单

控制台要包含：

1. 地图编辑器：摆放建筑、NPC、怪物、灵草、矿石、传送点、出生点。
2. NPC 编辑器：NPC 模板、立绘、对话、多分支选项、好感、任务、入队条件。
3. 怪物编辑器：怪物模板、名称、品阶、推荐境界、属性、生命、灵气、攻击、防御、图片、技能、音效、掉落、战斗背景。
4. 建筑编辑器：建筑模板、图片、碰撞、交互（商店、洞府、门派等）。
5. 场景编辑器：门派内部、洞府、城镇室内等独立场景。
6. 灵草编辑器：灵草、矿石、采集时间、刷新规则、掉落。
7. 丹方编辑器：丹方、材料来源、成功率、产物。
8. 物品编辑器：丹药、装备、法宝材料、消耗品。
9. 法术编辑器：五行属性、伤害、灵气消耗、冷却、Buff、特效、声音。
10. 功法编辑器：层级、学习条件、回合效果、属性加成。
11. 秘境编辑器：地形、节点、奖励、秘境令牌、失败离开规则。
12. 法宝编辑器：属性、技能、祭炼配置。
13. Buff 编辑器：增益/减益、持续回合、叠加规则。

### 4.3 模板与地图实例的区别（必须遵守）

以怪物为例：

- **怪物编辑器**创建“青木狼”模板，定义它的完整属性、图片、技能、掉落。
- **地图编辑器**只选择“青木狼”模板，然后在地图某个坐标放置一只实例。
- 地图实例只保存模板编号和坐标，不复制一大堆数值。
- 当“青木狼”模板修改后，地图上的所有青木狼自动更新。
- 当前角色击败某只怪物时，应记录该“地图实例编号”，而不是删除怪物模板。

NPC、建筑、物品、灵草等后续也遵守相同模式。

## 5. 当前项目文件与已实现内容

### 5.1 主要目录

```text
F:\webGame\
├─ index.html                 # 网页入口
├─ styles\main.css            # 全局样式
├─ public\assets\            # 图片、地图、角色等资源
├─ src\
│  ├─ core\                   # 全局状态、场景编号、数据仓库
│  ├─ scenes\                 # Phaser 场景
│  ├─ ui\                     # 独立 HUD / 弹窗等界面组件（不放地图逻辑）
│  └─ utils\                  # 可复用 UI 工具
├─ server.cjs                  # 本地网页服务器
├─ 启动游戏.bat                # 双击启动游戏的快捷文件
└─ 项目开发说明.md             # 本文件
```

### 5.2 已完成的可运行功能

- 水墨仙侠封面，包含踏入仙途、设置、开发者控制台。
- 最多 5 个角色档案：创建、进入、删除、本地保存。
- 角色创建：名字、性别、五行 10 点分配。
- 青云山 5×5 地图拼接和按需加载。
- 角色八方向移动（基于用户提供的五方向待机/行走素材并镜像补全），鼠标点击移动，人物阴影。
- 第一章古玉剧情与固定战斗。
- 水墨山景战斗界面：生命条、灵气条、五行圆盘、行动栏、战斗日志。
- 回合制普通攻击、五行术法、防御、敌方回合。
- 开发者控制台：地图编辑器、怪物编辑器已可进入，其他编辑器入口已预留。
- 地图编辑器：中键平移、滚轮缩放、全图加载、摆放/删除/保存对象。
- 怪物编辑器：怪物模板、数值、技能、掉落、图片上传、音效地址。
- 游戏内可读取地图编辑器放置的怪物；靠近按 E 进入战斗；获胜后怪物在当前角色档案中标记为已击败。
- 游戏内 NPC 当前可按 E 阅读默认对话；完整 NPC 编辑器尚未完成。
- 封面点击“踏入仙途”现在**必定先进入五个角色档案位**；选择已有角色才进入地图，点击空位才创建角色。
- 网页直接刷新时，`BootScene` 会读取最近游玩的角色并续玩地图进度；这与封面入口是两条不同流程。
- 已补上地图场景中“操作提示文字未初始化”导致的 `VillageScene.js` 报错，避免地图打开后只显示背景。
- Pixso“第一章地图”页面的顶部六个原始图标、人物资料底板、头像、小地图已整理到 `public/assets/images/ui/pixso-chapter-map/`。
- 第一章地图 HUD 已从地图场景抽离为独立组件 `src/ui/ChapterMapHud.js`；地图移动、NPC、战斗等逻辑仍保留在 `VillageScene.js`。
- 已新增 `server.cjs` 和 `启动游戏.bat`：双击 bat 文件即可在 `http://localhost:8000` 启动游戏。

### 5.3 当前关键文件

- `src/main.js`：注册所有 Phaser 场景。
- `src/core/GameState.js`：角色档案、当前角色、第一章和世界进度。
- `src/core/SceneKeys.js`：所有场景名称。
- `src/core/MapContentStore.js`：地图上放置对象的本地数据。
- `src/core/MonsterStore.js`：怪物模板的本地数据。
- `src/scenes/DeveloperConsoleScene.js`：全部编辑器总入口。
- `src/scenes/MapEditorScene.js`：青云山地图编辑器。
- `src/scenes/MonsterEditorScene.js`：怪物模板编辑器。
- `src/scenes/VillageScene.js`：青云山探索、移动、NPC/怪物互动。
- `src/ui/ChapterMapHud.js`：第一章地图的左上资料栏、顶部六功能图标、右侧任务/小地图、左下附近 NPC 信息。
- `src/scenes/BattleScene.js`：独立回合制战斗。
- `src/scenes/CoverScene.js`：封面和“踏入仙途 → 角色档案选择”的入口流程。
- `server.cjs`：本地开发服务器。

## 6. 建议的下一步开发顺序

> 本节保留早期产品规划。当前实际接手顺序已更新，请优先看第 0 节和第 13.8 节。

1. **第一章地图 UI 继续还原**：以用户提供的 `F:\游戏素材\第一章地图.jpg` 为唯一尺寸和排版参考，继续完善清晰度、头像裁切、面板笔触、各图标位置；目前已完成独立 HUD 架构与基础布局，但用户认为视觉清晰度和排版尚未达到原稿效果，不能视为完成。
2. **NPC 编辑器**：NPC 名称、立绘、对话、多分支、好感、任务条件；地图编辑器改为选择 NPC 模板放置。
3. **建筑编辑器**：建筑模板、图片和基本互动；地图编辑器改为选择建筑模板放置。
4. **怪物编辑器第二版**：怪物专属战斗立绘、技能特效、技能冷却、怪物 AI、战斗背景选择。
5. **法术编辑器**：将玩家和怪物技能全部数据化，不再把技能公式写死在 `BattleScene.js`。
6. **背包/物品/掉落系统**：怪物掉落进入待领取列表，背包整理后领取。
7. **任务与对话分支**：第一章完整剧情、支线、NPC 好感与入队。
8. **修炼与突破**：境界、小境界、灵根、功法、突破小游戏。
9. **世界势力、门派、灵兽、城市、秘境等大型系统**。

## 7. 两台电脑继续开发的交接方法

1. 复制整个项目文件夹，而不是只复制 `src`；`.git`、`public\assets`、脚本、文档和未提交文件都必须一起复制。
2. 两台电脑不要同时修改同一个版本。开始前先确认拿到另一台电脑最新文件。
3. 浏览器角色存档、地图编辑器已放置内容、怪物模板保存在浏览器本地，不会随着项目文件自动复制。
4. 游戏设置中已经接入本地资料导出/导入；换电脑前导出 JSON，换电脑后通过“导入资料”恢复。
5. 家里新的 Codex 任务开始时，请先让它阅读本文件，并告诉它：

```text
请先完整阅读项目根目录的 AGENTS.md、项目开发说明.md 和架构规范.md，
再运行 git status --short 与 npm test。请继续以 HTML + JavaScript + Phaser.js 开发《玄穹问道》，
保留当前未提交改动，并遵守最新模块边界与开发顺序。
```

6. 在另一台电脑双击 `启动游戏.bat`，然后访问 `http://localhost:8000`。运行期间命令窗口必须保持开启。

## 8. 今日开发记录（2026-08-21）

### 9.1 本次完成/修复

- 游戏画布已统一升级为 1920×1080；封面、角色档案、角色创建、大地图 HUD、战斗和编辑器使用统一 Full HD 显示换算，地图世界坐标与旧角色存档位置保持不变。
- 新增本地服务器与一键启动文件，解决 `localhost:8000` 因服务器未运行而无法访问的问题。
- 封面大图原始尺寸为 5504×3072，超过部分显卡的 WebGL 安全贴图上限；已保留原图，并生成 `xuanqiong-wendao-cover-2048.jpg` 供游戏使用，避免黑屏、绿屏或只有背景色。
- 修复 `VillageScene.updateNearbyInteraction()` 对未创建的 `operationHint` 调用 `setText()` 引起的场景中断错误。
- 按“场景与界面分离”原则，新建 `src/ui/ChapterMapHud.js`；`VillageScene.js` 只负责第一章地图玩法和交互，HUD 排版独立维护。
- 顶部储物袋、法术、功法、法宝、存档、设置图标改为单独资源加载，并保留点击入口。
- 封面的“踏入仙途”改为始终进入五档角色选择，避免从封面直接跳过角色选择进入旧存档。

### 9.2 尚未完成（交接时必须知道）

- 用户已经明确指出：第一章地图 UI 目前仍不够清晰，且布局和 `F:\游戏素材\第一章地图.jpg` 原稿不完全一致。下一台电脑继续开发时，优先处理这个问题；不要声称已经完全还原。
- 当前的 `ChapterMapHud.js` 是可维护的独立 UI 起点，后续应依据原图逐项微调坐标、大小、头像裁切、面板纹理和字体效果。
- 用户暂时说“先不管清晰度”，因此本次不要继续花时间修改清晰度，应先根据用户下一条需求继续。

## 9. 当前限制与注意事项

- 当前数据使用 `localStorage`，只存在当前浏览器和当前电脑；不是云端数据库。
- 目前怪物图片上传保存在浏览器本地模板中。图片太大时会占用较多浏览器存储，建议先使用适中尺寸的 PNG/WebP。
- 目前地图碰撞、自动寻路避障、NPC 专属立绘、怪物专属立绘、技能特效等仍属于后续功能。
- 当前编辑器是功能原型，UI 可以后续按用户提供的参考图逐步美化；先保证数据和游戏逻辑正确。

## 10. 2026-08-22 至 2026-08-23 开发记录与公司电脑交接

### 已完成并可继续测试

- 第一章大地图已按 1920 × 1080 设计基准持续调整；角色信息、顶部功能按钮、附近修士、当前任务、任务日志、小地图、方向箭头及界面遮罩已完成基础交互。
- NPC 管理编辑器已支持基本信息、头像与对话立绘分别上传、灵根、属性、技能、分支对话和保存；栖霞村村长已接入地图、资料卡、交谈及主线任务流程。
- 云游商人已接入地图附近提示、人物资料卡、购物入口、买入和卖出、商品库存、购物清单、数量调整、确认购买、灵石和库存变化。
- 控制台已新增物品管理编辑器，可编辑灵草、丹药、书籍、功法、装备、材料和其他物品；支持图片、品阶、商店设置、效果字段、筛选和保存。
- 储物袋已拆分成独立界面，已接入分类、品阶筛选、物品格子、名称、悬浮信息、高亮、右键操作菜单、整理排序、灵石和容量显示。
- 已处理游戏右键浏览器菜单的拦截，避免出现“保存图片”等网页菜单。

### 仍需优先验收

- 功法、书籍、闭关尚未形成完整可验收玩法，明天不能按“已完成”处理。
- 储物袋右键的使用、查看详情、丢弃及丹药使用后的数量和属性变化，需要完整测试。
- 新建物品、保存、返回后数据是否保留，曾出现异常，需要优先验证。
- 商人关闭后再次打开、反复进入人物资料和购物页面，曾有卡住现象，需要回归测试。
- 任务引路箭头、任务目标、接任务后的提示仍需按真实流程检查。
- UI 图片、文字、间距、圆角、颜色还有可继续按效果图微调的空间；功能修复和美术微调请分开进行。

### 明天到公司电脑

1. 复制整个 `D:\webGame` 文件夹，不要只复制源码；素材、`public`、编辑器文件和本说明都要带走。
2. 浏览器本地数据不会随文件夹自动复制。公司电脑第一次打开后，NPC、物品、任务和灵石测试数据可能需要重新建立；后续再补数据导入导出。
3. 先验证最小流程：控制台新建物品并保存，返回重进确认仍在；地图靠近商人购买 1 个物品，确认灵石与储物袋变化；打开储物袋检查分类、右键菜单和整理。
4. 上述流程稳定后，再继续做书籍、功法与闭关。每完成一小项都先在游戏画面测试，再进入下一项。

## 11. 统一弹窗 UI 规范（2026-08-24）

为避免“每做一个弹窗就重新设计一遍”，项目已新增独立组件：
`src/ui/XianxiaDialog.js`。今后新增**确认、提示、设置、奖励、任务接取、系统说明**等小型弹窗时，必须优先复用它，而不是在场景文件里临时绘制一套新面板。

### 视觉规则

- 统一使用：墨绿半透明全屏遮罩、深木色主体、鎏金双层边框、简洁回纹四角、金色标题与玉璧分割线。
- 按钮语义固定：**青绿**＝确认/安全保存；**蓝灰**＝普通设置；**金棕**＝工具、导入导出；**赤褐**＝关闭、退出、删除等危险操作。
- 所有弹窗自带右上角 `×` 关闭键、底部状态提示文字、淡入淡出和轻微缩放动画。
- 按钮采用“视觉层 + 独立透明点击区”方式。这样浏览器缩放、Phaser 容器动画后，按钮仍然能稳定点击，不会点击穿透到地图。

### 已接入的界面

- 第一章大地图的“游戏设置”弹窗：进入全屏、窗口化、导入/导出资料、保存退出、关闭。
- 顶部“法术 / 功法 / 法宝”等暂未开放功能的说明弹窗。
- 游戏封面的“设置”弹窗。

### 给后续开发者的调用方法

```js
import { XianxiaDialog } from "../ui/XianxiaDialog.js";

const dialog = new XianxiaDialog(this);
dialog.open({
  title: "获得奖励",
  subtitle: "古玉微光凝成一枚灵石",
  body: "是否收入储物袋？",
  buttons: [
    { label: "收入储物袋", variant: "primary", onClick: () => dialog.close() },
    { label: "暂不领取", variant: "secondary", onClick: () => dialog.close() },
  ],
});
```

复杂界面如储物袋、商店、战斗、NPC 全屏资料页属于“功能页面”，不是小型弹窗；但它们后续新增的确认框、关闭按钮、删除按钮仍应沿用本规范的颜色与组件。

## 12. 第一章地图右侧 HUD 水墨规范（2026-08-24）

- 日期、当前任务、小地图统一使用 `src/ui/ChapterMapHud.js` 中的“水墨卷轴信息栏”：深墨绿半透明外壳、宣纸内衬、淡墨晕染、金线回角。
- 小地图统一称为“观山镜”，使用金边圆形探索图；不要再改回黑色矩形底配大块荧光绿色探索区域。
- 右侧 HUD 的字体使用清晰无粗描边中文字体，任务标题用金色、任务正文用暖白色、寿命用淡玉绿色。
- 如未来增加天气、阵营、地区信息，应在右侧以同款小卷轴继续向下/向左扩展，不要新建现代风格的纯黑卡片。

## 13. 2026-08-24 至 2026-08-25 架构规范化完整交接

### 13.1 本轮目标与不可改变的原则

本轮不是单纯把大文件按行数切小，而是解决 `StorageBagPanel`、`VillageScene`、`BattleScene`
中页面、规则和存档互相耦合的问题。用户已经明确：

- 代码可以超过 1186 行，项目不设置任何强制行数上限。
- 文件是否合理只看职责是否单一、依赖方向是否正确、结构是否清楚、规则是否能独立测试。
- 功法、法术、商店、背包、法宝必须分开实现，不能继续互相借用页面方法。
- 新开发必须遵守 `AGENTS.md` 与 `架构规范.md`，不能退回旧式堆叠架构。

固定依赖方向为：

```text
scenes / ui  →  domain  →  core 数据仓库
```

详细强制规则不在本文重复维护，以 `架构规范.md` 为唯一架构细则来源。

### 13.2 新增的仓库级约束和验证入口

- 新增 `AGENTS.md`：要求所有后续开发者和 Codex 先读项目说明与架构规范，保护用户未提交改动。
- 新增 `架构规范.md`：记录依赖方向、各领域职责、存档迁移、奖励编辑器和后续拆分顺序。
- 新增 `scripts/check-architecture.mjs`：自动阻止领域层依赖 Phaser/UI、场景重新接管商店、战斗、
  奖励、存档迁移或怪物掉落编辑等职责。
- 新增 `package.json` 测试入口。统一命令为 `npm test`，目前依次运行：
  `check:architecture`、`test:artifact`、`test:combat`、`test:reward`、`test:save`。
- 架构检查明确不统计代码行数，也不会因为文件超过 1186 行失败。

### 13.3 角色菜单、背包、功法、法术与法宝

- `src/ui/character/CharacterMenuPanel.js` 成为角色属性、储物袋、法宝、法术、功法的公共外壳，
  只管理全屏生命周期、一级导航和子页切换。
- `src/ui/StorageBagPanel.js` 已变为纯背包子页，不再持有一级导航，不再实例化功法或法宝页面。
- `src/domain/inventory/InventoryService.js` 负责物品数量、使用、丢弃、成功率、恢复、临时效果和到期回滚。
- `src/domain/items/ItemCatalog.js` 是商店、背包、功法、法宝、奖励共同使用的统一物品目录。
- `src/domain/techniques/TechniqueLoadoutService.js` 与 `src/ui/techniques/TechniquePanel.js` 独立负责功法拥有、
  主修/四辅修/速度位、去重装备和卸下。
- `src/domain/spells/SpellService.js` 与 `src/ui/spells/SpellPanel.js` 独立负责法术查询和展示；法术页禁止读取功法配装字段。
- `src/domain/artifacts/ArtifactLoadoutService.js` 与 `src/ui/artifacts/ArtifactPanel.js` 独立负责六类法宝槽位。
- `GameState` 和 `ItemStore` 已增加 `equippedArtifacts` 与法宝类别；物品编辑器已经支持法宝分类。

### 13.4 商店与地图场景

- `src/domain/shop/ShopService.js` 统一处理玩家货币、商人货币、玩家背包、商人库存和买卖事务。
- `src/ui/merchant/MerchantPanel.js` 是商品区、购物车、数量、确认弹窗和购物会话状态的唯一所有者。
- `VillageScene` 只装配 `ItemCatalog`、背包、功法、法术、法宝、商店服务与对应 UI，不再保存
  `merchantCart`、`merchantMode` 等商店镜像字段，也不再声明商店兼容方法。

### 13.5 战斗、奖励与实际到账

- `src/domain/combat/CombatEngine.js` 统一负责先手、动作锁、随机伤害、灵气、敌方技能、冷却、防御、
  回合和胜负；`BattleScene` 只负责动画、音效、战斗日志和场景跳转。
- `src/domain/rewards/BattleRewardService.js` 负责灵石、修炼经验、物品入包、怪物击败记录、章节精英推进
  和重复结算保护。
- 已修复旧行为：过去战斗只显示“掉落”，实际上没有给玩家；现在日志展示的是服务返回的实际到账结果。
- 测试战斗不发奖励、不推进任务，并恢复玩家状态。
- 内置掉落“低阶材料、狼皮、土灵晶、低阶回灵丹、蚀月盟令牌残片”已登记到 `ItemStore`，可以真实进入储物袋。
- 未登记的旧掉落不会伪装成已获得，会显示“未登记，未入包”。

### 13.6 怪物掉落编辑器

- 新增 `src/domain/rewards/RewardCatalog.js`，把“灵石、修炼经验”和物品目录组合为统一奖励目录。
- 新增 `src/ui/editor/MonsterDropEditorPanel.js`，支持目录选择、分页、数量增减、重复奖励合并、删除和应用。
- `MonsterEditorScene.editDrops()` 不再使用自由文本 `window.prompt`。
- 旧怪物模板的“名称 × 数量”数组仍能读取；未登记文本可以显示、保留或删除，但不能从新面板新增。
- 当前持久化仍按名称保存，所以同名物品暂时去重。下一阶段需要升级为稳定奖励 ID，彻底消除改名和同名歧义。
- 已在本地浏览器实际点击验证：打开面板、分页、选择修炼经验、数量从 1 调为 3、加入、应用均正常，
  浏览器控制台没有错误。测试没有点击“保存所有怪物模板”，不会污染浏览器模板数据。

### 13.7 存档 schema v2 与迁移保护

- 新增 `src/core/save/SaveMigrations.js`，成为默认角色数据、当前版本和迁移注册表的唯一来源。
- 单个角色存档版本为 v2，五档位容器版本为 v2；迁移采用逐版 `vN → vN+1` 注册。
- `GameState.loadFirstChapterProgress()` 不再散落补字段代码，只装入迁移模块返回的标准数据。
- 当前稳定浏览器键为 `xuanqiong-wendao-save-slots`；旧 `xuanqiong-wendao-save-slots-v1` 和旧单档键
  只读兼容。迁移成功后复制到稳定键，不删除旧键。
- 未来版本或损坏容器会触发保护性写锁：读取失败后，本次运行不能把空档覆盖回原始数据。
- 商店库存中的 `0` 表示售罄，迁移会保留；背包中的零数量才会清理。
- 本地备份 JSON 的 `version` 是备份文件格式版本，与角色存档 schema 版本相互独立。

### 13.8 测试、当前状态和明天下一步

当前测试文件：

- `scripts/smoke-artifact-service.mjs`
- `scripts/smoke-combat-engine.mjs`
- `scripts/smoke-battle-reward-service.mjs`
- `scripts/smoke-save-migrations.mjs`

截至交接时，`npm test`、全部 `src/scripts` JavaScript 语法检查和 `git diff --check` 均通过。
换电脑后必须先重新运行，不要只相信本文记录。

下一步严格按以下顺序：

1. 建立 `domain/quests` 章节任务领域服务，把 `qingyunInvestigation`、引路开关、接取、完成和奖励推进
   从 `VillageScene` / HUD 中迁出；先写纯 JavaScript 状态流测试，再接场景。
2. 将怪物掉落持久化从“名称 × 数量”升级为 `{ rewardId, quantity }`，为怪物模板建立迁移；系统奖励使用
   固定 ID，物品奖励使用物品 ID。必须继续读取旧文本模板，不能直接废弃玩家已有怪物数据。
3. 上述边界稳定后，再继续 NPC 任务、完整法术数据化或其他玩法；不要重新把规则写回场景和页面。

### 13.9 当前工作区特别提醒

- 当前架构重构尚未提交到 Git；另一台电脑必须复制整个目录或先由用户自行提交/同步。
- `git status --short` 中的 `11.txt` 删除、`_codex_technique.png`、本地数据备份 JSON 等可能属于用户资料；
  不得擅自恢复、删除或加入业务改动。
- 行尾检查会提示部分文件未来由 LF 转为 CRLF，这是 Git 行尾提示，不是测试失败。
- 不要为了“文件看起来小”继续机械拆分；只在职责、依赖、复用或测试边界确实需要时拆分。

## 14. 地图编辑器多地图改版（2026-08-25）

- `src/scenes/MapEditorScene.js` 已改为左侧分类素材库、中央地图画布、底部操作区、右下全局视图的布局。
- 编辑器支持建筑、NPC、怪物、传送点放置，支持选中、删除、取消选择、撤销、重做、保存和导出配置；
  灵草、矿石、触发器的分类入口已预留，待对应内容仓库建立后接入。
- 新增 `src/core/MapCatalog.js` 作为地图目录。当前包含“青云山”和“乱星海”，两张地图使用稳定 ID、
  独立草稿、独立撤销历史和独立本地保存内容。
- 青云山继续使用现有 5×5 瓦片；进入编辑器时采用铺满画布的缩放，点击“全局视图”可查看完整地图。
- 项目内暂时没有乱星海底图，因此乱星海当前显示海色网格占位画布，但已经可以切换、放置和保存对象。
  后续取得正式底图后，只需在地图目录中改为正式资源配置，不应再重写地图切换逻辑。
- 地图底部标签和固定工具栏已阻止点击穿透，切换地图不会误放置对象。
- 已在本地浏览器实际验证青云山与乱星海切换、画布适配和对象数量隔离；控制台无错误。

## 15. 2026-08-25 当日完成与次日交接（最高优先级）

> 本节记录本工作日已落地的功能、仍需继续的边界和接手顺序。换电脑后必须先阅读本节，
> 并以本节覆盖第 13.8 节中已经完成的旧任务顺序。

### 15.1 今日已完成的功能

#### 角色、立绘与角色档案

- 新增 `src/core/PortraitCatalog.js`，统一管理可选立绘的稳定 ID、名称、性别和资源路径；不要在场景中
  手写立绘路径或用显示名称当作数据 ID。
- 新增五张可选立绘至 `public/assets/images/portraits/`：店小二、猎户、女散修、书生、长者。
  `CoverScene` 会预加载完整立绘目录。
- `CharacterCreateScene` 已重做为创建角色界面：点击头像进入横向选择立绘界面，中间选中的立绘放大，
  左右可切换；确认后把 `portraitId` 写入角色数据，性别也随所选立绘同步。
- `SaveMigrations` 已把旧档规范为带 `player.portraitId` 的数据；旧档没有该字段时默认使用
  `cultivator-female`，因此升级不会破坏旧档。
- `SaveSlotScene` 中角色档案卡会从完整立绘裁成圆形头像；不再要求额外上传一张头像图。
- `ChapterMapHud` 与 `VillageScene` 已按当前 `portraitId` 显示地图左上角头像。纹理键使用立绘 ID，避免
  切换角色后误复用旧头像缓存。
- 游戏内“存档”页的手动存档卡也读取 `slot.snapshot.player.portraitId`；存档立绘已下移到信息分隔线附近，
  不会与顶部标题挤在一起。

#### 游戏内菜单、存档与界面稳定性

- 角色菜单的属性、储物袋、法宝、法术、功法、社交、存档已继续统一为同一套顶部导航和居中排版；
  存档页的“删除存档”已放在“保存游戏”旁边，并对按钮组做水平居中。
- 新增/完善 `src/ui/save/`、`src/domain/save/` 与 `src/core/save/SaveArchiveRepository.js`：支持档位选择、
  手动保存、删除、自动存档间隔和反馈提示，UI 不直接处理存档规则。
- 属性页已使用 `src/ui/character/AttributePanel.js` 与 `src/domain/character/` 的角色资料服务承载字段，
  避免继续把属性计算写回场景或 `render()`。
- 已修复角色菜单在“属性 / 功法”等标签间快速来回切换时，短暂露出地图的点击穿透/显示闪烁问题；
  菜单覆盖层和导航事件现在由 HUD 正确截获。

#### 封面、控制台、地图和内容编辑器

- 封面、角色档案、创建角色、控制台均已按当前仙侠视觉重新排版，按钮文字以按钮中心对齐为原则。
- `DeveloperConsoleScene` 已扩展为后续编辑器入口；地图、物品、怪物、NPC、建筑等编辑功能保留为独立入口，
  后期可以继续增加门派、采收等编辑器，不应把它们塞进一个页面的业务逻辑中。
- `MapEditorScene` 和 `MapCatalog` 已支持青云山、乱星海两个稳定地图 ID，分别保存草稿/撤销历史/放置内容。
  乱星海暂用网格占位底图，等待正式素材。
- 物品、怪物、NPC 编辑器已经统一为内容管理式三栏布局。怪物编辑器支持独立立绘上传、清除、自定义图片
  状态提示和保存反馈；对应图片逻辑由 `src/core/MonsterAppearance.js`、`src/utils/ImageStorage.js` 管理。
- 怪物掉落已使用 `MonsterDropEditorPanel` 和奖励目录编辑，不再用自由文本弹窗；旧文本掉落仍保持兼容。

#### 建筑模板编辑器（本次最后完成项）

- `src/scenes/BuildingEditorScene.js` 已重做为和 NPC/物品管理一致的三栏编辑器：左侧模板列表，中央为
  “基本信息 / 碰撞范围 / 交互设置 / 预览”页签，右侧是资源与保存操作。
- 建筑模板目前可配置：稳定模板 ID、名称、分类、宽高、显示锚点、图片、阻挡开关、碰撞形状、
  手绘多边形碰撞点、交互开关、交互类型、标题、提示语和目标 ID。
- 碰撞范围可在网格预览中用鼠标逐点绘制，存储为相对图片坐标（0 到 1）；缩放显示尺寸时碰撞数据仍可复用。
  旧 `blocked` 数据自动兼容为四点矩形，不会丢失已有模板。
- `WorldTemplateStore` 已规范保存 `display`、`collision`、`interaction` 字段，同时保留旧
  `interactionText` 兼容字段。
- `VillageScene` 已实际使用建筑碰撞多边形阻挡角色移动，判定使用 `Phaser.Geom.Polygon.Contains`；这部分
  不只是在编辑器里显示数据。

### 15.2 本日新增或重点模块索引

- 角色立绘：`src/core/PortraitCatalog.js`、`public/assets/images/portraits/`、
  `src/scenes/CharacterCreateScene.js`、`src/scenes/SaveSlotScene.js`、`src/ui/ChapterMapHud.js`。
- 存档：`src/core/save/SaveMigrations.js`、`src/core/save/SaveArchiveRepository.js`、
  `src/domain/save/`、`src/ui/save/`、`scripts/smoke-save-archive-service.mjs`。
- 属性与任务：`src/domain/character/`、`src/ui/character/AttributePanel.js`、`src/domain/quests/`、
  `scripts/smoke-character-profile-service.mjs`、`scripts/smoke-chapter-quest-service.mjs`。
- 战斗快捷栏：`src/domain/combat/CombatShortcutService.js`、`scripts/smoke-combat-shortcut-service.mjs`。
- 建筑：`src/scenes/BuildingEditorScene.js`、`src/core/WorldTemplateStore.js`、
  `scripts/smoke-building-template.mjs`。
- 多地图与本地资料：`src/core/MapCatalog.js`、`src/core/LocalDataTransfer.js`、
  `scripts/smoke-local-data-transfer.mjs`。

### 15.3 当前验证基线

本次交接前已在项目根目录运行 `npm test`，全部通过。它依次包含：

- `check:architecture`
- `test:quest`
- `test:transfer`
- `test:artifact`
- `test:shortcut`
- `test:archive`
- `test:profile`
- `test:building`
- `test:combat`
- `test:reward`
- `test:save`

修改 JavaScript 后，除 `npm test` 外，仍要对新增/修改的 JavaScript 做语法检查，并运行
`git diff --check`。换电脑后第一轮也必须重新做这三项验证，不要只相信本文结果。

### 15.4 次日接手的推荐顺序

1. 先按第 0 节完成完整迁移、`git status --short`、`npm test`、本地数据导入和浏览器启动确认；不要重置
   当前工作区，也不要遗漏未跟踪的 `public/assets/images/portraits/`、`src/domain/`、`src/ui/save/` 等目录。
2. 优先把建筑模板的 `imageData` 与 `display` 接到 `MapEditorScene` 和 `VillageScene` 的真实建筑渲染。
   **现状：**建筑编辑器能上传/保存图片，角色移动也会按手绘碰撞阻挡；但地图/探索中仍以现有通用建筑
   标记或旧绘制方式展示，尚未根据模板图片完整渲染建筑立绘。实现时必须通过模板 ID 读取数据，不能把
   图片 Base64 或碰撞规则复制进场景。
3. 再把建筑的 `interaction.kind`（商店、传送、场景、门派等）与稳定 `targetId` 接到各自领域服务。
   当前文本交互兼容仍可用，但正式商店、传送、门派跳转不能靠解析提示文字实现。
4. 之后根据用户继续提出的编辑器需求，增加门派编辑器、采收系统编辑器或更多地图素材分类。每个编辑器
   都要按“仓库/领域规则 → 场景装配 → UI 绘制”的方向实现，并配最小可重复冒烟测试。
5. 怪物掉落的持久化仍有一个技术债：旧结构以名称保存。后续升级为 `{ rewardId, quantity }` 时要写迁移、
   继续兼容旧数据，并在 `RewardCatalog` 中用稳定 ID 去重；不要急于删除旧名称数组。

### 15.5 当前工作区与迁移提醒

- 当前工作区包含大量已修改和未跟踪文件，均可能是本日功能的一部分或用户已有资料。复制时必须复制
  整个目录，包含 `.git` 和未跟踪文件；接手者只能检查差异，不能执行会清空工作区的 Git 命令。
- 浏览器 `localStorage` 不会随目录迁移。换机后请使用“设置 → 导入资料”导入原电脑导出的备份 JSON；
  如需保守操作，先在原机导出一次最新资料并保留原文件。
- 新电脑上的 Codex 在任何改动前都必须完整阅读 `AGENTS.md`、`项目开发说明.md` 和
  `架构规范.md`。其中 `架构规范.md` 是强制依赖边界，不是可选参考文档。

## 16. 建筑碰撞编辑易用性调整（2026-08-26）

- `BuildingEditorScene` 的碰撞画布不再默认“点击图片就新增顶点”。默认工具改为“拖动编辑”，
  直接拖动橙色编号节点即可调整碰撞边界，避免误点产生大量难以整理的小顶点。
- 只有主动点击“添加顶点”后，点击建筑图片才会增加顶点；画布顶部会明确显示当前工具状态。
- 增加“底部矩形”预设与“重新识别轮廓”操作。一般建筑建议先使用自动轮廓或底部矩形，再拖动
  少量节点微调，不需要从零开始手绘多边形。
- 顶点控制点已置于图片点击层上方，能正常拖拽；拖动过程中边界和半透明阻挡范围即时更新，松开鼠标后
  顶点列表再同步刷新。原有相对坐标存储、旧档兼容和实际移动阻挡规则保持不变。
- 上传建筑图片后会读取透明通道，自动生成最多八个、贴近建筑外缘的初始碰撞点，并默认启用阻挡；
  用户只需拖动少量节点修正细节。若图片本身没有透明背景，则自动使用贴近完整图片边缘的初始范围。
- “添加顶点”现为一次性插入：新点自动插到距离点击处最近的边上，随后回到拖动编辑，避免按点击先后
  把多边形连成交叉乱线。需要恢复自动结果时可点“重新识别轮廓”。

## 17. 地图编辑器素材栏易用性调整（2026-08-26）

- 地图编辑器左侧素材栏已由 282px 加宽到与物品管理一致的 348px，分类按钮改为两行完整名称，
  地图相机、底部地图标签和点击边界同步适配，不会遮挡或穿透。
- 模板不再只显示文字小按钮，而是使用纵向管理卡片：怪物读取怪物立绘，NPC 读取 NPC 立绘，
  建筑读取建筑图片；卡片同时显示名称及品阶、境界、属性等摘要信息。
- 怪物分类进入时默认不选任何模板。用户必须先点击怪物卡片，之后才能在地图上放置；未选择时点击
  地图只显示提示，避免误放第一只怪物。
- 模板超过五个时可在素材列表区域使用鼠标滚轮或上下箭头浏览，不影响地图画布的滚轮缩放。
- 已在本地浏览器验证默认未选择、点击地图提示、模板卡高亮和画面布局，控制台无错误。

## 18. 地图建筑真实图片与拖动（2026-08-26）

- 新增 `src/core/BuildingAppearance.js`，地图编辑器与探索场景统一通过建筑模板 ID 读取图片、显示宽高和锚点；
  地图实例仍只保存模板 ID 与坐标，不复制 Base64 图片。
- 地图编辑器中的建筑不再显示为圆点。已上传图片的建筑会按建筑管理中的 `display.width`、
  `display.height` 和 `display.anchor` 显示真实建筑图，选中时显示覆盖整张建筑的金色边框。
- 建筑图片的整个矩形范围都可命中，不必再精确点击底部小圆点。按住鼠标左键可拖动 NPC、怪物、建筑
  和传送点；建筑拖动时坐标与显示位置实时更新，松开鼠标记为一次可撤销操作，之后点击“保存到游戏”生效。
- `VillageScene` 同样读取建筑模板图片和显示尺寸，所以地图编辑器保存后，正常探索地图不会重新显示为圆点。
- 已在本地浏览器验证建筑放置、选中、拖动、坐标更新与松开提示；测试未点击保存，没有写入测试建筑。

## 19. 地图对象实例缩放（2026-08-26）

- 地图编辑器选中任意已放置对象后，左侧提供“缩小 / 当前百分比 / 放大”操作；支持建筑、NPC、怪物、
  灵草、矿石、传送点和触发点，范围为 25% 到 400%，每次调整 25%，点击百分比可恢复 100%。
- 缩放值保存在地图实例的 `scale` 字段中，不修改建筑、NPC 或怪物模板，因此同一模板放到地图上的不同
  实例可以使用不同大小；旧地图数据自动补为 100%。
- 编辑器显示、选中命中范围和建筑拖动边框都会同步缩放；`VillageScene` 的实际地图立绘、NPC 问号标记、
  怪物图片、建筑图片以及建筑碰撞多边形也使用同一比例。
- `scripts/smoke-map-content.mjs` 覆盖旧数据默认值、有效比例和 25%～400% 边界兼容。

## 20. 开发者控制台项目文件持久化（2026-08-26）

### 20.1 已完成

- 开发者控制台的物品、怪物、NPC、建筑模板，以及地图编辑器放置内容，已不再写入浏览器
  `localStorage`；统一通过本地开发服务器写入项目目录。
- 模板资料文件位置：
  - `data/editor/items.json`
  - `data/editor/monsters.json`
  - `data/editor/npcs.json`
  - `data/editor/buildings.json`
  - `data/editor/map-content.json`
- 控制台上传的图片不再以 Base64 塞进浏览器或 JSON，而是写入
  `public/assets/images/editor/items/`、`monsters/`、`npcs/`、`buildings/`。JSON 中保存的是
  项目内图片 URL，游戏、地图编辑器和内容编辑器共用同一张图片。
- 新增 `src/core/EditorFileRepository.js` 作为前端统一仓库入口，`server.cjs` 新增
  `/api/editor-data` 接口，文件落盘与 Data URL 图片转换集中在 `server/editorFileStorage.cjs`；
  UI、场景不直接操作文件系统。
- 初次打开某一类编辑器时：若项目里尚无对应 JSON，会自动读取该浏览器的旧模板一次并迁移到
  项目文件。旧浏览器副本暂时保留为安全备份，但之后所有新的控制台保存只写项目文件。
- 若本地服务器尚未重启、仍是旧版接口，读取项目资料会失败；此时仓库必须回退显示旧浏览器资料，
  不能误显示默认模板。已在 `ItemStore`、`MonsterStore`、`WorldTemplateStore` 与
  `MapContentStore` 中修复该保护逻辑。
- 怪物编辑器已兼容项目图片 URL，不会把 `/public/assets/...` 当作 Base64 导致预览失败；所有
  “浏览器空间不足”提示已改为“无法写入项目文件夹/确认本地服务器运行”。
- 设置中的资料导入导出现在只处理角色档案、游戏设置等玩家浏览器资料，不再把控制台模板写回
  浏览器。控制台资料随整个 `webGame` 项目目录移动。

### 20.2 使用与换电脑规则

1. 必须通过 `启动游戏.bat` 打开 `http://localhost:8000`；直接双击 `index.html` 没有本地服务器，
   控制台保存会明确提示失败。
2. 之后在控制台点击“保存全部/保存到游戏”后，刷新页面仍会从项目 JSON 读取；上传图片可在上述
   `public/assets/images/editor/` 目录看到对应 `.webp` 文件。
3. 换电脑时复制**整个** `webGame` 文件夹（包含 `data/`、`public/`、`.git` 和未跟踪文件），即可带走
   所有控制台模板、地图摆放和上传图片；角色档案仍需要通过“设置 → 导出资料/导入资料”迁移。
4. 若需手动检查数据，优先查看 JSON，不要编辑 `localStorage`。手工改 JSON 前先备份原文件；图片文件名
   由模板 ID 和字段名生成，再次上传同一模板图片会覆盖对应图片文件。

### 20.3 验证与下一步

- 新增 `scripts/smoke-editor-file-storage.cjs`，覆盖模板 JSON 写入、图片 Data URL 落为项目图片文件、
  读取回写和地图内容保存；`npm test` 已包含 `test:editor-files` 且本轮通过。
- 本轮已运行 `npm test` 和全部相关 JavaScript 的 `node --check`，均通过。
- 下次实际验收顺序：启动 `启动游戏.bat` → 在物品、怪物、NPC、建筑各上传一张小图并保存 → 地图编辑器
  放置/保存一个对象 → 刷新游戏确认内容仍在 → 检查 `data/editor/` 与
  `public/assets/images/editor/` 是否出现对应文件。
- 本轮已从 Git 中保留的 `2026-08-24` 历史浏览器备份恢复：60 个物品、51 张物品图片、3 个 NPC
  模板及其立绘、2 个怪物、1 个建筑和地图摆放资料。恢复结果已实际写入项目文件并通过本地 API
  读取与图片访问验证；恢复脚本为 `scripts/recover-editor-backup.cjs`，它发现 `data/editor/`
  已有内容时会拒绝覆盖。
- NPC 管理的头像优先使用历史资料中已裁好的方形 `avatarData`；若只有竖版立绘才自动截取顶部中央。
  裁切后的显示比例按裁切区域计算，避免 `setDisplaySize` 按整张立绘缩放而把头像缩小、变形。
- NPC 管理的列表头像和“立绘自动裁切头像”预览均已恢复真正的圆形蒙版：头像以圆心居中显示，
  超出圆周的头发、衣服等像素会自动隐藏，并给圆框描边保留 2px 内边距。头像与立绘分别使用独立
  Phaser 纹理键，历史资料中两张不同图片不会再互相串用；重新上传立绘时，旧头像纹理会同步失效并刷新。
- 竖版立绘的头像定位不再使用 Phaser `setCrop` 的原图原点，而是先把顶部中央的头肩区域精确对准圆心，
  再交给圆形蒙版裁切，避免圆圈内只显示衣服或只剩半张头像。
- 探索地图镜头调整为 `0.88`：背景地图、主角及地图内物件会同步缩小约 12%，一次可看到更多地形；
  这是纯视觉镜头调整，不修改地图坐标、角色存档位置、建筑碰撞或编辑器摆放数据。探索场景已分为
  世界镜头和 1:1 UI 镜头，顶部功能图标、角色资料、右侧任务/小地图、弹窗等固定界面不会跟着缩小或移位。

## 21. 建筑遮挡排序与双向碰撞（2026-08-26）

- 主角、NPC、怪物仍按脚底世界 Y 坐标动态排序；建筑统一放在更低的地景层，并在建筑之间按碰撞轮廓
  最下沿排序。建筑任何位置的围墙、山石和透明边缘都不能盖住人物，保证角色始终完整可见。
- 新增 `domain/world/BuildingCollisionService.js`，把建筑相对碰撞点、地图实例尺寸与缩放转换成世界坐标；
  场景只负责传入本次移动的起点和终点。
- 碰撞由“只检查下一帧落点”升级为“检查整条移动线段是否进入或穿过边界”，从下向上、从上向下及
  单帧跨越建筑都会阻挡。旧存档若已经把角色留在建筑内部，仍允许向外移动脱困。
- `scripts/smoke-building-collision-service.mjs` 覆盖双向进入、大步跨越、外侧移动、实例缩放和内部脱困。

## 22. 地图模板一次性放置（2026-08-26）

- 建筑、NPC、怪物和传送点模板默认都不选中，必须主动点击模板卡片才进入放置状态。
- 模板采用一次性放置：地图上创建一个实例后立即取消模板高亮并退出放置状态，不会因随后点击空白处
  意外复制出第二个对象。
- 点击或拖动任意已有地图对象、切换分类、切换地图、撤销、重做和“取消选择”都会清除待放置模板。
- 已放置对象处于选中状态时，点击地图空白只取消对象选择，不创建新实例；再次点击模板卡片才可继续放置。

## 23. 门派入口、令牌准入与内部总览（2026-08-26）

- 玩家靠近门派建筑碰撞边缘 320 世界单位时，建筑中央会自动出现紧凑棕色圆角“进入”按钮，离开范围自动
  隐藏，不再要求先点击建筑。距离按碰撞边缘而非建筑中心计算，因此巨型建筑也能正常触发；若建筑只有一部分
  位于镜头内，按钮会自动限制在可视区域并避开右侧 HUD。无准入资格时明确提示完成任务或寻找接引人。
- 入口按钮已改为 128×58 的低调棕色圆角样式，去掉建筑上方重复的门派名；文字对象明确使用中心原点，
  “进入”在水平与垂直方向都保持居中，不再出现图 1 中偏右、偏下的问题。
- 新增 `core/SectCatalog.js` 作为门派配置目录，门派建筑匹配、准入任务、令牌、附近接引人、门内修士、
  功能入口和未来任务/宝库/传送接口均由配置描述；新增其他门派时不需要把分支继续写进 `VillageScene`。
- 新增纯 JavaScript `domain/world/SectAccessService.js`：进入条件为“完成该门派准入任务”或“持有门派令牌”
  二者之一，令牌只用于身份判定，不会在进入时消耗。门派任务进度使用存档 v4 的
  `world.completedQuestIds` 与 `world.sectProgress`，旧档会自动迁移。
- 天剑宗旁 560 世界单位内的村长模板会被识别为“天剑宗接引人”。玩家可按 E 或点击 NPC 开始对话；
  首次完整对话后通过背包领域服务获得一枚“天剑宗令牌”，随即显示通用“获得物品”弹窗。令牌属于
  不可使用、不可出售的系统任务物品，刷新与重新进入游戏后仍保留，重复对话不会重复发放。
- 新增 `SectScene` 与 `ui/sect/SectOverviewPanel.js`。天剑宗内部总览目前提供“炼丹房”和“闭关室”两个
  可点击入口，并保留宗门任务、门人名录、宗门宝库、门派传送的服务 ID 接口；Esc 或“返回大地图”可原路返回。
- 新增 `scripts/smoke-sect-access-service.mjs`，覆盖门派建筑匹配、附近接引人半径、令牌首次发放、防重复、
  任务准入和系统物品规则；已纳入统一 `npm test`。

## 24. 地图 NPC 单字识别标记（2026-08-26）

- 尚未配置大地图立绘的 NPC 仍使用“问号 + 圆牌”交互提示；圆牌不再显示无意义的数字 `1`，
  而是自动显示角色名称的第一个字。例如“天剑宗接引人”显示“天”、村长显示“村”。
- 商人无论模板名称如何，圆牌统一显示“商”，便于玩家一眼识别可购物角色；该文字仅为地图视觉标记，
  不会改动 NPC 模板名称、对话、身份或存档资料。

## 25. 门派内部共用角色顶栏（2026-08-26）

- 新增 `ui/PlayerTopToolbar.js`，把左上角色头像、姓名、境界、生命/修为条以及顶部六个功能图标整理成
  大地图与独立玩法场景共用的 UI 组件；大地图原有坐标、尺寸、素材和点击行为保持不变。
- 天剑宗内部不再绘制横跨全屏的深色标题栏，也不再使用单独的小型角色资料框；进入门派后直接显示与
  大地图完全一致的角色顶栏和“储物袋、法术、功法、法宝、存档、设置”六个入口。
- “天剑宗”及副标题已从屏幕中央移到顶部最右侧并采用右对齐，不再占用角色资料与功能图标之间的空间。
- 顶栏资源预加载也集中到共用入口，之后更换大地图图标或资料底板时，门派内部会同步使用同一版本。

## 26. 天剑宗炼丹房与闭关室（2026-08-26）

### 26.1 炼丹房

- “炼丹房”已由占位说明升级为完整独立页面。首次进入中央显示“添加丹炉”，可在玄铁、灵火、玄冰、
  地心、九阳五类丹炉中选择或更换；丹炉分别提供成丹率与额外成丹率加成，选择结果保存在
  `world.sectProgress[sectId].alchemy`，刷新后仍会保留。
- 新增 `core/AlchemyCatalog.js` 作为丹炉与丹方稳定配置，新门派可以继续追加自己的丹炉、丹方和材料，
  不需要把配置写进 UI。当前丹方直接复用物品管理中的真实灵草、丹方书和丹药 ID。
- 新增纯 JavaScript `domain/alchemy/AlchemyService.js`：学习进阶丹方会检查背包是否持有对应丹方书；
  开炉前统一检查丹炉、已学丹方和全部材料，条件不足不会误扣。炼制开始后按丹方扣除灵草，成功产物
  通过背包服务真实入包，失败则保留明确结果；炼制次数与成功次数会保存。
- 宗门提供一份可直接学习的入门回灵丹方，便于第一次使用。筑基丹、结金丹等进阶内容已预留真实丹方书、
  材料和产物关联，后续增加物品只需扩充配置。

### 26.2 闭关室

- “闭关室”已升级为独立蓝色静修页面，中央使用当前玩家立绘与修为进度，左右可切换“法术 / 功法”，
  并可选择 1 月、3 月、6 月或 1 年的闭关时长。
- 新增 `core/RetreatCatalog.js` 和纯 JavaScript `domain/cultivation/RetreatStudyService.js`。学习会检查真实秘籍、
  最低闭关时长和重复学习；成功后增加修为、记录累计闭关月份，并把对应功法物品加入背包，同时写入
  `learnedTechniques`，因此现有法术页和功法配装会直接识别，不另造一套孤立数据。
- 当前接入《清心诀残卷 → 清心诀》和《火球术要略 → 火球术》两条完整链路。后续新增秘籍时使用
  `sourceBookId`、`learnedItemId` 和 `kind` 配置即可自动进入相应分类。
- 领悟结果会显示独立的出关反馈层；修为变化会立即刷新门派顶栏。Esc 会优先关闭丹炉选择/心境试炼，
  再返回宗门总览，不会直接误退出大地图。

### 26.3 验证与维护

- 新增 `scripts/smoke-alchemy-service.mjs`，覆盖未放丹炉、未学丹方、缺少丹方书、材料校验、真实扣料、
  成丹入包和宗门炼丹进度。
- 新增 `scripts/smoke-retreat-study-service.mjs`，覆盖法术/功法分类、秘籍校验、最短时长、修为收益、
  学习结果入现有系统、累计月份与防重复学习；两项均纳入统一 `npm test`。
- `UiHelpers.addText` 现在会真正把 `origin` 应用到 Phaser 文字对象，而不是误放进文字样式。炼丹、闭关及
  现有按钮中声明了居中的文字会按预期居中。
- 已在本地浏览器按“进入天剑宗 → 炼丹房 → 添加/更换丹炉 → 学习丹方 → 材料不足保护 → 闭关室 →
  法术/功法切换 → 时长选择 → 缺少秘籍提示”完整检查，1280×720 缩放布局无重叠，控制台无运行错误。

### 26.4 三阶段控火小游戏（2026-08-26）

- 炼丹不再点击“开炉炼丹”后立即随机结算。药材入炉后会进入全屏控火小游戏，必须依次完成“温炉、
  融药、凝丹”三个阶段；小游戏未结束前绝不会生成丹药。
- 玩家可长按鼠标“催火”按钮或空格键提高炉温，松开后炉温下降。每个阶段有不同目标温区、升温速度、
  降温速度和倒计时；持续保持在目标区域会积累该阶段的药性稳定度。
- 凝丹阶段稳定三秒后“收诀凝丹”才会亮起，玩家必须主动点击。倒计时结束仍未收诀会强制炸炉，不能靠
  基础成功率跳过操作。最终评分综合三个阶段的温区保持比例、凝丹时炉温和是否主动收诀。
- 控火成绩分为“天成、上品、稳定、勉强、炸炉”。高分提高最终成丹率和额外成丹率，低分降低成功率，
  炸炉直接失败；丹炉与丹方本身的成功率仍参与最终领域结算。
- 新增 `domain/alchemy/AlchemyMinigameService.js` 负责温度、阶段、难度和评分，新增
  `ui/sect/AlchemyMinigamePanel.js` 负责按键、动画、温度条和结果画面。高阶丹方会缩窄安全温区，
  但 UI 不直接计算数值规则。
- `AlchemyService` 已拆成 `beginRefinement` 与 `completeRefinement`：开始时一次性校验并扣除药材，小游戏
  评分有效后才能结算。旧式 `refine()` 入口被明确禁止，重复结算、无效评分和演练伪装真实炼制均会失败。
- 炼丹房增加“控火演练”：使用当前丹方和丹炉练习全部三个阶段，但不消耗药材、也不产生丹药，便于
  玩家熟悉手感。放弃真实炼制不会退回已经入炉的材料。
- 新增 `scripts/smoke-alchemy-minigame-service.mjs`，覆盖催火升温、自然降温、禁止提前凝丹、阶段推进、
  主动收诀、超时强制失败和高阶丹方温区收窄；炼丹事务测试同时覆盖“未完成小游戏不产出”。
- 已在本地浏览器验证完整小游戏布局和超时失败结果页，1280×720 缩放下没有遮挡，控制台无运行错误。

### 26.5 三境闭关小游戏（2026-08-26）

- 闭关不再点击“开始闭关”后立即学会秘籍，而是进入“调息、守心、铭法”三段心境试炼。只有心境评分
  达到 55 分，才会把对应法术或功法加入现有 `learnedTechniques` 和背包物品系统。
- 调息阶段的蓝色呼吸环会持续收放，玩家需在它与金色心环重合时点击“吐纳”或按空格键，共完成三次；
  每次偏离心环的程度都会计入调息契合度。
- 守心阶段包含四道心诀，每道会同时出现三种心念。选错不会卡死流程，但会降低守心评分，避免只靠等待
  跳过操作。
- 铭法阶段先逐个闪现符文，展示结束后玩家需按原顺序复现。闭关 1、3、6、12 个月分别对应 3、4、5、6
  道符文，闭关越久，记忆难度越高；铭法倒计时结束仍未完成会强制判定领悟失败。
- 最终心境分为“澄明、入定、凝神、心乱、走火”。前三档可领悟秘籍并按等级修正修为收益；心乱或走火
  不能学会秘籍，只保留少量静修修为。真实闭关失败仍会消耗所选月份，提前出关则不消耗月份。
- 闭关室增加“静心演练”。已经领悟、暂时缺少秘籍的内容也能演练完整流程，但演练不消耗闭关时间、
  不增加修为且不会重复发放功法，便于反复熟悉操作。
- 新增纯 JavaScript `domain/cultivation/RetreatMinigameService.js` 负责阶段、计时、符序和评分；
  `RetreatStudyService` 拆为 `beginStudy`、`completeStudy`、`abortStudy`，旧式 `study()` 已禁止直接结算，
  重复结算和用演练结果冒充真实闭关都会失败。
- 新增 `ui/sect/RetreatMinigamePanel.js` 负责动画、点击、倒计时和结果画面；闭关时长、秘籍 ID 与最终学习
  结果仍沿用现有配置和系统接口，后续新增功法/法术不需要为小游戏另写一套规则。
- 新增 `scripts/smoke-retreat-minigame-service.mjs`，覆盖精准吐纳、阶段推进、禁止提前铭法、长闭关符序、
  低分失败与超时失败；闭关事务测试覆盖未完成小游戏不学习、失败少量修为、演练无奖励和放弃无消耗。
- 已在本地浏览器完整操作三段演练并得到“入定”结果，1280×720 缩放布局无重叠，控制台无运行错误。

### 26.6 炼丹房视觉重构（2026-08-26）

- 炼丹房主页面已由三块同权重的棕色矩形改为“丹方录 / 丹炉灵台 / 炼制筹备”三级结构。中央丹炉成为
  唯一主视觉，左右面板分别承担选择和行动，避免视线在三个大框之间反复跳动。
- 页面底色收敛为墨黑与深木色，金色只用于选中、边框和主标题，玉色用于成功率、材料充足和丹炉增益，
  朱砂色用于主操作与材料不足；状态颜色具有固定语义，不再用大面积亮棕色铺满页面。
- 丹方卡增加左侧选中标记、品质、掌握状态和基础成功率条；选中不同丹方时，右侧名称、说明、综合成功率、
  材料清单和操作按钮同步更新，原有数据与服务调用保持不变。
- 丹炉区域增加聚火阵纹、灵火呼吸光效、品质标签、成丹/额外产出双统计卡，以及开炉次数、成功次数、
  最佳控火成绩。动画仅属于 UI 表现，不参与任何炼丹数值计算。
- 炼制筹备区按“丹方信息 → 综合成丹预估 → 入炉灵药 → 丹方来源 → 主操作”的阅读顺序重新排版；
  材料行会明确显示充足/不足和拥有量/需求量，真实开炉使用朱砂主按钮，演练与工具操作使用深色次按钮。
- 丹炉选择弹窗同步使用内外双层金边、圆角炉卡和统一状态色，并保留玄铁、灵火、玄冰、地心、九阳五类
  丹炉的原有选择与存档逻辑。
- 本次只修改 `ui/sect/AlchemyRoomPanel.js` 表现层，没有改变丹方、材料消耗、成丹率、控火小游戏或存档规则。
  已在本地浏览器验证丹方切换、丹炉更换、缺少丹方书反馈和 1280×720 缩放布局；修复一个不兼容的图形
  绘制调用后，页面没有新增控制台错误。

### 26.7 门派页面刷新恢复（2026-08-26）

- 门派总览按 F5 刷新后会回到当前门派总览，不再固定跳回青云山大地图；炼丹房和闭关室刷新后也会回到
  对应房间。
- 页面位置使用当前浏览器标签页的 `sessionStorage`，并与最近游玩的角色档位绑定，不写入角色存档，
  不会让另一角色误用当前页面。点击“返回大地图”或真正进入探索场景后会清除恢复位置。
- 正在进行中的控火或心境小游戏不会恢复半途中间状态；刷新后只回到所属房间，避免材料、修为和奖励事务
  被重复结算。
- 新增 `core/SceneResumeState.js` 和 `scripts/smoke-scene-resume-state.mjs`，覆盖门派总览、两个房间、档位隔离、
  损坏记录与主动清除，并纳入统一 `npm test`。

### 26.8 闭关室普通清修（2026-08-26）

- 闭关室中央新增“开始闭关”清修入口，与右侧的秘籍参悟分离；普通清修不要求持有秘籍，也不会学习法术
  或功法，只按选择的 1 月、3 月、6 月、1 年时长逐步增加修为。
- 1/3/6/12 个月分别使用 12/24/40/60 秒的界面闭关时间，完整完成可获得 60/195/420/900 修为；数值统一
  保存在 `RetreatCatalog`，后续平衡时无需修改 UI 或领域规则。
- 清修过程中显示闭关进度、已得修为、目标修为和预计剩余时间；修为按进度分段写入角色并刷新顶部与中央
  修为条。玩家可以提前出关，已经获得的修为保留，但不累计完整闭关月份。
- 新增 `domain/cultivation/CultivationRetreatService.js` 和 `scripts/smoke-cultivation-retreat-service.mjs`，负责
  逐步增长、时长校验、提前出关、完整结算、存档与防重复；UI 只负责定时推进和显示。

### 26.9 Pixso 页面 2 闭关室重制（2026-08-26）

- `ui/sect/RetreatRoomPanel.js` 已按 Pixso 页面 2 的“闭关室、闭关室-法术、闭关室-学习中、
  闭关室-学习成功”四个 1920×1080 画板重制。主视觉、圆形法术/功法入口、时长滑轨、565×764 秘籍弹窗、
  240×195 秘籍卡、学习进度层和成功层均使用画板坐标、尺寸、配色与间距。
- Pixso 导出的打坐人物、六本秘籍、火球术成功图和背景已保存在
  `public/assets/images/pixso/retreat/`；标题字体保存到 `public/assets/fonts/` 并通过 `styles/main.css` 注册。
  原始四画板 HTML 参考保存在 `design-reference/pixso-page2/retreat-page2.html`，后续对照调整无需重新猜尺寸。
- 法术入口现在打开六格“法术秘籍”弹窗。当前《火球术要略 → 火球术》沿用真实背包、秘籍与法术系统；
  崩山拳意、玄冰碎玉手、伏虎金刚劲、焚天炎龙破、九霄神雷诀保留稳定展示 ID、境界门槛和素材接口，
  未接入真实秘籍前点击会明确提示，不会伪造学习结果。功法入口继续接入现有《清心诀》链路。
- `RetreatStudyService` 新增定时参悟事务：选择秘籍后只创建学习进度，单次推进最多计入一秒；进度未满时
  禁止结算，完成后才将法术/功法加入 `learnedTechniques` 和背包。成功层显示三秒后自动回到闭关室，
  Esc 可关闭秘籍弹窗或放弃进行中的学习，但成功反馈必须自然播放完成。
- 普通清修时长同步为 Pixso 的 1/10/30/50 年四档，完整收益为 3600/36000/108000/180000 修为；实际等待
  仍压缩为 12/24/40/60 秒，修为逐步增加并允许提前出关，避免点击后瞬间结算。
- `scripts/smoke-retreat-study-service.mjs` 增加定时学习的半程、禁止提前结算、完成与防重复覆盖；
  `scripts/smoke-cultivation-retreat-service.mjs` 同步验证四档新配置。统一 `npm test` 已通过。

## 27. 2026-08-26 当日完整汇总与下次交接（当前最高优先级）

> 本节是 2026-08-26 收工时的统一交接入口，汇总当天散落在第 16～26 节的全部成果、当前边界和
> 下一次开发顺序。后续开发若与第 13.8、15.4 节的旧任务顺序冲突，以本节为准；具体实现细节仍回看
> 对应章节和源码中文注释。

### 27.1 编辑器图片、WebP 转换与项目文件持久化

- 物品、NPC、怪物和建筑编辑器现在统一接收 PNG、JPG、WebP。浏览器只读取用户选择的原文件，按用途
  等比缩小并转换成 WebP 后再保存，不会修改磁盘上的原图；物品图标使用较小尺寸，怪物/NPC 立绘保留
  更高分辨率，建筑图最高边按 1024px 处理，兼顾清晰度、读取速度和项目体积。
- 图片压缩与转换集中在 `utils/ImageStorage.js`，不是分别在各编辑器复制一套算法。建筑图片还会读取
  透明通道，生成最多八个、可继续拖动微调的碰撞轮廓；没有透明背景时安全退化为图片边缘范围。
- 控制台模板与地图摆放已从 `localStorage` 迁到 `data/editor/*.json`，上传图片落在
  `public/assets/images/editor/`。前端通过 `core/EditorFileRepository.js` 访问，文件写入集中在
  `server/editorFileStorage.cjs`；场景和 UI 不直接操作文件系统。
- 保存后 F5 会重新读取项目 JSON 与 WebP 文件，解决“点一下才刷新、强制刷新图片又消失”和大图把
  浏览器存储撑满的问题。必须通过 `启动游戏.bat` 或 `node server.cjs` 启动；直接打开 `index.html`
  没有本地 API，编辑器会提示无法写入项目文件夹。
- 首次读取某类资料时会尝试迁移旧浏览器模板；本轮已从历史备份恢复 60 个物品、51 张物品图片、
  3 个 NPC、2 个怪物、1 个建筑及地图摆放资料。恢复脚本发现 `data/editor/` 已有内容时会拒绝覆盖。

### 27.2 NPC、怪物立绘和地图头像

- NPC 只需上传一次立绘。编辑器列表和预览会从立绘自动生成头像，历史数据若已有专门的方形头像则优先
  使用；立绘与头像采用独立纹理键，换图后会使旧纹理失效，避免串图、变形和刷新后回到旧图。
- 大地图“附近修士”头像固定为 60×60、8px 圆角方框，使用铺满后裁切的头部特写，不再显示被压扁的
  半身照或圆形头像。NPC 管理页内部的圆形列表头像仍使用真正的圆形蒙版，两种使用场景不要混用。
- NPC 模板同时保留 `mapPortraitData`：大地图人物立绘与对话立绘/头像职责分离；尚未配置地图立绘时继续
  显示问号圆牌，圆牌以 NPC 名首字或“商”作为识别，不伪造角色素材。
- 怪物编辑器上传的立绘已经统一用于编辑器卡片、地图实例和正式战斗，不再由地图、战斗各自使用旧图。
  `core/MonsterAppearance.js` 已预留 `static` / `animated` 外观模式、动态图集配置和静态回退图接口；
  当前只实现静态立绘，后续接动态怪物时扩展渲染器即可，不能删除静态回退。

### 27.3 地图编辑器与对象操作

- 地图编辑器改为 348px 宽的内容管理式素材栏，建筑、NPC、怪物模板使用带图片和摘要的卡片；模板列表
  可独立滚动，不会抢走地图缩放。青云山与乱星海使用稳定地图 ID、独立摆放数据和撤销历史；乱星海
  目前仍是海色网格占位，尚未接正式底图。
- 建筑不再是圆点，地图编辑器与大地图都按模板 ID 读取真实建筑图片、宽高、锚点和碰撞数据；NPC、
  怪物、建筑、传送点都可直接拖动。地图实例仍只保存模板 ID、坐标和实例覆盖字段，不复制模板图片。
- 所有地图对象均可按实例缩放，范围 25%～400%、每次 25%，点击百分比恢复 100%；建筑图片、碰撞、
  NPC 标记和怪物立绘同步缩放，同一模板的不同实例可使用不同大小。
- 模板改为一次性放置：新建一个实例后立即退出放置状态；选择/拖动已有对象、点击空白、切换分类或地图、
  撤销和重做都会清除待放模板，避免整理地图时误点一下又复制新建筑。
- 建筑编辑器的碰撞工具默认是拖动节点，只有主动选择“添加顶点”才会一次插入新点；提供自动透明轮廓、
  底部矩形和重新识别。实际探索碰撞使用独立 `BuildingCollisionService` 检查整条移动线段，从上、下或
  单帧跨越均不能穿入，旧档中已经在建筑内的角色仍允许向外脱困。
- 人物按脚底 Y 排序，建筑位于地景层；建筑透明边缘、屋檐和围墙不应再把主角整个人盖住。此策略优先
  保证角色可见，不做建筑前后分层的伪 3D 遮挡。

### 27.4 门派入口、接引人与天剑宗总览

- 天剑宗建筑会在玩家距离碰撞边缘 320 世界单位内自动显示 128×58 的“进入”按钮；按钮文字居中，
  巨型建筑只露出一部分时也会限制在当前可视区并避开右侧 HUD，不再要求先点击建筑。
- 门派配置集中在 `core/SectCatalog.js`，准入判断集中在 `domain/world/SectAccessService.js`。进入条件为
  完成准入任务或持有令牌二选一，令牌只作身份判断、不消耗；其他门派可复用同一配置与服务接口。
- 天剑宗接引人不是场景里临时写死的假 NPC，而是由 NPC 模板和附近配置识别。首次完整对话发放一枚不可
  使用、不可出售的“天剑宗令牌”，通过通用获得物品弹窗显示，重复对话不会重复发放。
- `SectScene` 已有门派总览、门内修士列表、炼丹房和闭关室；宗门任务、门人名录、宗门宝库、门派传送
  保留稳定服务 ID，当前尚未实现内容。F5 会按角色档位恢复门派总览、炼丹房或闭关室，不会跳回大地图；
  正在进行的小游戏/学习事务不恢复半途状态，避免重复结算。
- 门派页面与大地图共用 `PlayerTopToolbar`，角色头像、姓名、境界、生命/修为和六个功能入口保持一致；
  “天剑宗”标题位于右上角，不再挤占顶部中间区域。

### 27.5 炼丹房

- 炼丹房已接真实丹炉、丹方书、材料、产物、背包和宗门进度。五类丹炉、入门回灵丹方以及筑基丹、
  结金丹等接口由 `AlchemyCatalog` 配置；学习、扣料、入包和防重复结算由 `AlchemyService` 负责。
- 炼丹不能点击后直接成功。真实炼制会先一次性检查并扣除药材，再进入“温炉 → 融药 → 凝丹”三阶段
  控火小游戏；玩家控制炉温、保持目标温区并在凝丹稳定后主动收诀，超时会炸炉。小游戏评分再与丹炉、
  丹方成功率合并结算，未完成小游戏绝不生成丹药。
- “控火演练”可完整练习流程，但不扣材料、不产丹。真实炼制开始后主动放弃不会退回已经入炉的药材。
- 页面已经按“丹方录 / 丹炉灵台 / 炼制筹备”重构，金、玉、朱砂分别表达选中/增益/主操作与不足；
  这次视觉重构没有把数值规则重新写进 UI。

### 27.6 闭关室与 Pixso 页面 2

- 闭关室当前以 Pixso 页面 2 的四个 1920×1080 画板为准：闭关室主页面、秘籍选择、学习中、学习成功。
  对照 HTML、打坐人物、六本秘籍、成功图和专用字体均已保存到 `design-reference/pixso-page2/`、
  `public/assets/images/pixso/retreat/` 与 `public/assets/fonts/`，不要再凭感觉重做排版。
- 法术入口打开六格秘籍弹窗；《火球术要略 → 火球术》已连接真实秘籍、背包和法术系统，功法入口连接
  《清心诀残卷 → 清心诀》。其余五本只保留稳定展示 ID、境界门槛和素材接口，尚未接真实物品/技能；
  点击会明确提示，不得把占位卡写成已实现玩法。
- 学习采用定时事务：选择秘籍后进入学习中页面，进度未满不能结算；完成才写入 `learnedTechniques` 和
  背包，学习成功层自然显示三秒后回到闭关室。Esc 可关闭秘籍弹窗或放弃学习，但不能绕过事务直接领奖。
- 普通清修与秘籍学习分离。1/10/30/50 年四档在现实中压缩为 12/24/40/60 秒，完整收益为
  3600/36000/108000/180000 修为；修为随进度逐步写入，允许提前出关并保留已得修为。
- 早先实现的“调息、守心、铭法”三境闭关服务、UI 和测试仍保留，但 Pixso 重制后的当前秘籍主流程使用
  定时学习页面。以后若恢复三境玩法，应明确它是高阶领悟或演练模式，并确保只通过同一领域事务结算，
  不能与定时学习重复奖励。

### 27.7 架构、数据和验证基线

- 当天新增规则仍遵守 `scenes/ui → domain → core`：建筑碰撞、门派准入、炼丹事务、控火小游戏、闭关学习、
  普通清修、三境试炼均可在 Node 中独立验证；UI 只绘制、接输入和展示服务结果。
- 角色存档已迁移到包含门派任务、令牌、宗门炼丹和闭关进度的当前 schema；控制台模板属于项目文件，
  不再混入角色资料导入导出。页面刷新位置只存在当前标签页 `sessionStorage`，并按角色档位隔离。
- 2026-08-26 收工前重新运行 `npm test`，1 项架构检查和以下 20 组测试全部通过：章节任务、资料导入导出、
  法宝、战斗快捷栏、存档档案、角色属性、建筑模板、建筑碰撞、门派准入、页面恢复、炼丹、控火小游戏、
  闭关学习、普通清修、三境试炼、编辑器项目文件、地图内容、战斗、战斗奖励、存档迁移。
- 工作区仍包含大量已修改与未跟踪文件，全部可能属于本日功能或用户资料；当前没有替用户提交 Git。
  接手时只能检查差异，禁止 `git reset --hard`、`git checkout -- .` 或清理未跟踪目录。行尾的 LF/CRLF
  提示不是测试失败。

### 27.8 尚未完成与下一次开发顺序

1. 第一件事仍是完整阅读 `AGENTS.md`、本文和 `架构规范.md`，运行 `git status --short` 与 `npm test`，
   通过本地服务器启动；不得重置当前工作区。
2. 优先在真实游戏中对照 Pixso 四画板验收闭关室四种状态，至少检查 1920×1080 和 1280×720：主页面、
   六格秘籍弹窗、学习进度、三秒成功层。源码和自动测试已完成，但本轮没有把四种游戏内状态逐张做完
   最终截图对比，因此不能宣称像素级视觉验收已经完成。
3. 为崩山拳意、玄冰碎玉手、伏虎金刚劲、焚天炎龙破、九霄神雷诀补真实物品、法术/功法数据、境界校验
   与获取途径；接入时复用 `RetreatCatalog` 和 `RetreatStudyService`，不要在弹窗点击事件中直接发技能。
4. 完成乱星海正式底图、更多门派内容前，继续沿用稳定地图/门派配置接口；宗门任务、宝库、名录和传送
   目前只是预留接口。怪物动态立绘也只是外观接口，尚未实现动态图集播放。
5. 继续保留旧怪物掉落文本兼容；后续升级 `{ rewardId, quantity }` 时必须写模板迁移并使用稳定奖励 ID，
   不能直接删除玩家已有名称数组。
6. 下次新增功能后，除 `npm test` 外继续对改动 JavaScript 运行 `node --check`，并运行 `git diff --check`；
   浏览器实机验收结果要区分“已验证”和“仅代码/自动测试通过”。

## 28. 探索地图异步地图块闪屏修复（2026-08-27）

- 青云山按需加载新的地图块时，已在创建同一帧把地图块排除出固定 1:1 UI 镜头；它只会由跟随主角的
  世界镜头绘制。此前等待低频镜头同步的极短窗口，可能让新地图块以错误的屏幕坐标覆盖一次画面。
- UI 镜头现在会在首批地图块显示前创建，因此角色档案、右侧任务与小地图附近也不会被初始地图块或
  后续加载的地图块短暂覆盖；红框位置出现的局部地图闪动与普通移动闪屏使用同一修复路径。
- 该修复不改变地图块路径、世界坐标、主角位置、地图数据或存档，只消除角色移动进入新区域时偶发的
  地图闪屏。

## 29. 闭关按钮视觉校正与 NPC 门派身份选择（2026-08-27）

- 闭关室时长、开始闭关、离开闭关室和右上角房间按钮已继续对齐 Pixso 页面 2：开始按钮改为
  `#4162AE` 蓝底白字与 12px 圆角；离开按钮使用 `#27384B → #151D26` 深蓝渐变、`#485971`
  描边和 `#B2CBF7` 字色；时长按钮区分选中与未选中的蓝灰底色和文字色，不再继承金色通用按钮样式。
- `RetreatRoomPanel.makeButton` 新增可选渐变、透明度、字体、圆角和无描边支持，仍只负责绘制与输入，
  未改动普通清修的修为增长、计时、提前出关或存档规则。
- NPC 管理的“门派”和“身份”由自由文本框改为下拉选择。新建 NPC 默认显示“请选择门派 / 请选择身份”；
  当前门派项包含散修和 `SectCatalog` 中登记的全部门派，新增门派后编辑器会自动出现对应选项。
- 身份选项统一集中在 `core/NpcProfileCatalog.js`，包含村民、商人、接引人、各级弟子、执事、长老、
  掌门、炼丹师、炼器师和任务发布者等常用身份。旧 NPC 若保存过目录外的自定义门派或身份，打开时仍会
  自动保留为当前选项，不会因改成下拉框丢失资料。
- 本轮已对三个改动 JavaScript 文件运行 `node --check`，并完整运行 `npm test`；架构检查与 20 组冒烟
  测试全部通过。浏览器实测新建 NPC 页面出现境界、门派、身份三个下拉框，门派可选择天剑宗，身份可选择
  内门弟子；闭关按钮配色已按效果稿数值落地，但当前测试档没有准入令牌，未通过正式地图流程完成最终截图。

## 30. 小地图探索足迹闪屏修复（2026-08-27）

- 右下“观山镜”左边偶发的一闪绿影，不是地图块本身，而是主角移动后小地图新增探索足迹时，
  新创建的 UI 圆点在镜头低频同步之前被世界镜头额外绘制了一次。
- `ChapterMapHud` 现在让固定 UI、小地图迷雾、主角蓝点和每一个新增足迹在创建同一帧即排除世界镜头；
  并将移动中的足迹刷新由“销毁后重画全部”改为“仅追加本次新足迹”。这样红框区域和小地图本体都不会因为
  足迹重建而闪动，且降低长时间探索时的绘制开销。
- 小地图访问记录、世界坐标、地图素材和角色存档结构均未改变；进入旧档时仍会一次性重绘已有足迹。

## 31. 天剑宗总览背景替换（2026-08-27）

- 用户最终指定的 `F:\游戏素材\素材\建筑\宗门1.jpg` 已原样复制到
  `public/assets/images/sects/sect-tianjian-background.jpg`，作为门派总览的 1920×1080 背景。
- `SectScene` 保留稳定纹理键 `sect-mountain-background`，仅替换其文件路径；门内修士、宗门事务、
  炼丹房、闭关室、功能入口和全部存档/领域规则均未修改。

## 32. NPC 互动与任务配置重制（2026-08-27）

- NPC 管理原来的四个空白任务文本框已改为“互动与任务”向导，先选择“只进行对话 / 对话后赠礼 /
  发布任务”，再显示对应表单。编辑者不需要填写物品 ID，奖励直接从真实 `ItemCatalog` 下拉选择；
  门派令牌等不可出售的系统物品也会出现在同一个列表中，每个互动最多可配置三种物品与数量。
- “对话后赠礼”支持每个角色仅一次、玩家没有该物品时补发、每次对话都发放三种规则。奖励只在普通
  对话完整结束或分支对话走到结尾后结算；玩家主动返回地图中断对话不会得到物品。
- 右上角“一键设为天剑宗接引人”会同时设置门派为天剑宗、身份为接引人、互动模式为对话赠礼、奖励为
  天剑宗令牌、规则为缺少时补发，并登记天剑宗引导完成标记。保存 NPC 后，只需在对话编辑页写好台词，
  将该 NPC 模板放在门派附近即可完成“对话 → 获得令牌 → 进入天剑宗”的完整流程。
- 新增纯领域服务 `domain/quests/NpcInteractionService.js`：先完整校验全部奖励，再调用真实
  `InventoryService` 发放，使用 `world.completedQuestIds` 记录一次领取和可选完成标记。场景仅负责在
  对话结束时调用服务，并通过通用获得物品弹窗依次展示多种奖励；没有在 UI 内直接修改背包或存档。
- NPC 模板的 `quest` 结构新增 `mode`、`rewardItems`、`repeatPolicy`、`claimId` 与
  `completionQuestId`，旧版 `enabled/title/description/target/reward` 继续兼容，不会因打开新版编辑器而
  丢失。发布任务模式当前可保存名称、说明、目标和奖励；打怪、收集等自动完成事件仍须接入现有任务领域
  服务，界面已明确标注，不能把尚未接入的目标伪装为已可运行。
- 新增 `scripts/smoke-npc-interaction-service.mjs` 并纳入统一 `npm test`，覆盖无效物品不得部分发放、
  天剑宗令牌补发、一次性领取、完成标记和允许重复发放。浏览器已在 1280×720 实测三模式布局与一键
  预设回填：规则下拉为“没有时补发”，奖励为“天剑宗令牌 × 1”，画面无溢出和遮挡。

## 33. NPC 顺序对话与编辑器易用性修复（2026-08-27）

- 修复“编辑了多句台词，地图中只显示第一句”的问题。旧编辑器把每句话保存为互不相连的回复节点，
  节点又没有真实的主角选项与跳转目标；新版模板规范化会识别这种旧资料，把全部节点文字按原顺序安全
  迁移为连续台词，地图中按空格、E 或点击对话框会依次播放，最后一句结束后才触发对话赠礼。
- NPC 对话页现在先选择“顺序对话（推荐）”或“玩家选项分支（高级）”。顺序模式不再出现节点 ID、
  主角选择和跳转目标，只显示编号台词卡；支持直接多行输入、添加下一句、删除、上下调整、超过五句分页
  和统一保存，界面明确展示地图将按 `1 → 2 → 3` 播放。
- 高级分支继续保留给真正需要玩家回答的剧情。切换到高级模式会把原连续台词转换成可继续编辑的线性
  分支；切回顺序模式会保留全部 NPC 回复文字。高级模式移除了容易产生孤立节点的“新建回复节点”，
  改成“添加结束选项”与“添加选项和新回复”，并明确提示未连接回复不会自动播放。
- `WorldTemplateStore.normalizeNpc` 新增稳定 `dialogueMode`，自动清理未填写的空白主角选项；已有村长
  真实分支剧情仍保持分支模式，云游商人和普通 NPC 自动采用顺序模式。预览页也会根据当前模式读取正确
  的第一句，不再因分支模式的普通台词数组为空而显示省略号。
- 新增 `scripts/smoke-npc-dialogue-template.mjs` 并纳入统一 `npm test`，覆盖旧孤立节点迁移、顺序台词、
  真实分支、显式结束节点与空白选项清理。浏览器已在 1280×720 验证天剑宗接引人的五句台词完整显示、
  顺序/高级模式双向切换和文字保留；测试过程未点击保存，不会覆盖项目 NPC 资料。

## 34. 项目结构审查与章节、商店事务修复（2026-08-27）

- 完整复查了 `core / domain / ui / scenes` 的目录与导入关系，当前实际依赖仍符合
  `scenes / ui → domain → core`；领域模块没有依赖 Phaser、DOM、UI 或场景。
- 修复第一章结算页“重新体验第一章”只清除两个旧标记的问题。`ChapterQuestService.restartChapter()`
  现在一次性重置任务状态、引路、古玉、精英战斗标记与角色生命/修为，并只保存一次；结算场景不再
  直接修改这些字段。架构检查新增对应防回退规则。
- 修复商店出售时未校验商人灵石的问题。`ShopService` 会拒绝未知交易模式、非整数数量和商人资金不足；
  最终价格从真实物品目录重新计算，不再信任 UI 购物车携带的价格，双方货币、库存与背包仍按一次事务提交。
- 新增 `scripts/smoke-shop-service.mjs`，并扩充章节任务测试覆盖重新体验。当前 `npm test` 包含 1 项架构
  检查和 23 组冒烟测试，全部通过；本轮修改的 JavaScript 语法检查与 `git diff --check` 也通过。
- `README.md` 的启动说明已从不支持编辑器 API 的 `python -m http.server` 改为 `node server.cjs` 或
  双击 `启动游戏.bat`。
- 经用户确认后已逐文件核对并删除旧项目副本 `F:\webGame\webGame\`（约 112 MB）：原目录 691 个文件
  全部受 Git 跟踪，663 个与根项目相同，28 个在根项目中均有更新版本，没有旧目录独有文件；删除内容仍可
  从 Git 历史恢复。`.gitignore` 已加入 `/webGame/`，防止再次误把整个项目复制到项目根目录中。
- 下一批结构债务是把小地图探索足迹写入规则迁出 HUD，并把角色创建规则迁入领域服务。

## 35. 小地图探索足迹领域化（2026-08-27）

- 新增纯 JavaScript `domain/world/MapExplorationService.js`，统一负责“每移动 120 世界像素记录一次”、
  世界坐标取整、非法坐标拒绝、最多保留 1000 个最新足迹，以及旧档无效坐标的显式修复。
- `ChapterMapHud` 不再导入 `gameState`，也不再创建、追加或裁剪 `world.miniMapVisitedPoints`；HUD 只消费
  服务返回的不可变坐标快照、追加本次新足迹并绘制主角蓝点。达到容量上限时才按服务快照重画一次，
  日常移动仍保持只追加一个足迹的低开销路径。
- `VillageScene` 只在装配阶段创建服务并注入 HUD。旧档发生实际修复时只保存一次；正常走动产生的足迹
  继续跟随原有 5/10/15/30 分钟自动存档节奏，不会每走 120 像素就写一次 `localStorage`。
- 新增 `scripts/smoke-map-exploration-service.mjs`，覆盖首次记录、距离阈值、非法输入、查询引用隔离、
  旧档修复幂等和容量淘汰；架构检查同时禁止 HUD 重新直接访问探索存档。
- 本轮新增/修改 JavaScript 的 `node --check`、完整 `npm test` 和 `git diff --check` 均通过；统一测试现为
  1 项架构检查与 24 组冒烟测试。浏览器实际刷新进入青云山并点击移动后，观山镜蓝点与探索亮区正常更新，
  控制台无警告或错误；该实机步骤没有触发手动存档。
- 当前下一项结构债务：把角色创建的姓名、立绘/性别同步、五行十点分配与确认规则迁入独立角色领域服务；
  仍须先写纯 JavaScript 测试，再接 `CharacterCreateScene`。

## 36. 修为上限与突破前停滞（2026-08-28）

- 炼气阶段的修为上限现在是稳定字段 `cultivationExpTarget: 1000`。角色达到 `1000 / 1000` 后，
  经验绝不会继续累加；突破系统完成后只需提高该角色的目标值，所有经验来源会自动使用新上限。
- 新增纯领域模块 `domain/cultivation/CultivationProgressService.js`，由战斗奖励、可使用物品、普通清修、
  秘籍领悟和角色资料共同调用。奖励会返回实际获得值与溢出值，满值时界面明确提示“需要突破”，不会再显示
  虚假的全额经验。
- 普通清修在中途刚好到达瓶颈时会立即停止并保留本次实际所得；满修为时不能重新开始清修。秘籍仍可正常
  学会，但其附带经验只会填满剩余差值。纯经验物品在满修为时不会被消耗，避免无提示浪费。
- 角色存档升级到 v5：所有旧档的超额修为会自动裁切到目标上限，截图中的 `18961 / 1000` 重新进入角色后
  会变为 `1000 / 1000`。新增 `smoke-cultivation-progress-service.mjs`，并扩展战斗、闭关、秘籍和存档测试
  覆盖截断、溢出与旧档迁移。

### 36.1 闭关室突破入口（2026-08-28）

- 修为达到当前上限后，闭关室不再显示“开始闭关”和可获得修为，而会显示当前瓶颈及“突破修为”按钮。
- 炼气突破现为可操作的“灵息凝旋”：灵机穿过金色气穴时按空格或点击中央“凝”字，五次评分达到门槛才会成功；失败会跌落 10% 当前修为，成功才会存档、进入下一小境界并从 0 修为开始。
- 各大境界不能复用同一种玩法：筑基为地脉连阵、金丹为丹火控温、元婴为神魂符序、化神为神识穿越天劫裂隙。玩法目录位于 `domain/cultivation/BreakthroughTrialCatalog.js`；后续实现必须按各自 `type` 接入，不能改回点击即成功。
- 规则结算位于 `CultivationBreakthroughService.js`，小游戏规则位于 `BreakthroughTrialService.js`，视觉与输入位于 `ui/sect/BreakthroughMinigamePanel.js`；UI 不能直接修改角色境界、修为或失败惩罚。

## 37. 第一章可玩冒险循环（2026-08-28）

- 根据“现有游戏不好玩”的反馈，第一章主线不再是“接任务后走到古玉旁自动完成”。现在的完整短循环为：
  **接取村长委托 → 采集异光灵草 → 到雾岚岔路作选择 → 绕路获得小奖励，或进入浓雾战斗获得更高奖励 → 前往古潭取得古玉**。
  这是后续探索、材料、战斗、宗门解锁和突破资源的基础循环，而不是新增一个孤立面板。
- `domain/quests/ChapterQuestService.js` 新增稳定阶段
  `gather-herb / choose-path / defeat-guardian / find-ancient-jade / completed`，以及对应地图事件。
  只有前一阶段完成后才能推进下一阶段，不能通过场景坐标或重复按键跳过采集、抉择或战斗；旧的进行中档进入地图时会显式补为第一步，已完成档案保持完成状态。
- 地图场景在三个固定世界位置显示低依赖的符印锚点（异光灵草、雾岚岔路、古潭问道台）。接任务后自动打开引路，
  箭头、右侧任务目标和任务日志会随阶段同步更新，不会再出现“目标写采集灵草、箭头却指向古玉”的问题。
- 岔路提供可见取舍：稳妥绕行直接得到 `灵石 × 4` 并进入古潭；风险路线进入“雾隐山魈”战斗，胜利后得到
  `灵石 × 12` 与 `星萤果 × 1`，再继续古潭。战败或主动离开后任务仍停在“击败雾隐山魈”，回到岔路按 E 可以再次挑战，
  不会锁死任务。
- 已完成旧主线的角色可打开右侧“任务日志”，在已完成主线详情处点击“重新体验”。这会使用
  `ChapterQuestService.restartChapter()` 清空第一章任务进度并回到村长接取步骤，让已有档案可直接试玩新循环；
  不要求新建角色。
- 战斗使用 `BattleRewardService` 的稳定怪物 ID 防止刷新重复发奖；战斗胜利推进仍由章节领域服务确认。场景只负责
  世界输入、对话和场景跳转，奖励、阶段和存档修改没有放进 UI。
- 本轮已对 `ChapterQuestService.js`、`VillageScene.js`、`BattleScene.js` 与任务冒烟测试执行语法检查，并运行完整
  `npm test`；架构检查与全部冒烟测试通过。

### 下一步玩法方向

1. 将战斗奖励中的材料接到一张“回村交付 / 炼丹 / 兑换功法碎页”的可见选择上，让资源有立即用途；
2. 为不同地图区域配置 2～3 种随机事件模板（采集、精英战、商人、救援），但先复用现有任务和奖励领域服务；
3. 筑基、金丹等突破小游戏继续按 `BreakthroughTrialCatalog` 的不同玩法类型实现，不能回退为单按钮成功；
4. 开始任何后续修改前，必须依次阅读本文件和 `架构规范.md`，然后运行 `git status --short` 与 `npm test`。

### 37.1 古玉战斗选择卡死修复（2026-08-28）

- 古玉剧情原来把“进入战斗”放在普通对话的结束回调中，画面没有真实选择按钮；同时文案提示点击
  “返回地图”，但该按钮在普通对话打开时实际被隐藏，玩家容易停在最后一句而误以为游戏卡死。
- 第一次取得古玉和后续重复触碰古玉现在都使用真实双选项：`立即迎战 / 暂时离开`。选项可直接点击，
  也可按数字键；E 或空格会确认第一项并进入战斗，Esc 仍可安全关闭且不会触发战斗。
- 对话姓名标签分别显示“劫修来袭”和“古玉异动”，不再使用含义不明的默认“修士”。

### 37.2 进入战斗卡死修复（2026-08-28）

- 古玉选择能够正常触发；真正的中断发生在 `BattleScene.create()`。固定劫修没有编辑器怪物模板，
  场景会显式传入 `null`，但统一怪物外观解析器此前直接读取 `null.appearance`，导致 JavaScript 报错，
  画面只能停在旧对话或空背景。
- `MonsterAppearance.resolveMonsterAppearance()` 现在会把 `null`、旧档空值和未配置模板统一回退为空外观，
  固定劫修继续使用默认魔蛛序列帧，自定义怪物仍使用自己的上传立绘。修复位于统一外观模块，不在战斗
  页面为某一只敌人增加临时特判。
- 主角与魔蛛原有的两套 153 帧完整动作保持不变，没有删除或降低动画帧数。

## 38. 2026-08-28 当日完整汇总与下次交接（当前最高优先级）

> 本节是 2026-08-28 收工时的统一交接入口，汇总当天第 36～37.2 节及最后完成的角色创建领域化。
> 后续开发若与第 13.8、15.4、27.8 节的旧任务顺序冲突，以本节为准；具体实现细节仍回看对应章节和源码。

### 38.1 修为封顶、旧档迁移与所有经验来源统一

- 炼气阶段使用稳定修为上限 `cultivationExpTarget: 1000`。战斗、物品、普通清修和秘籍领悟全部通过
  `domain/cultivation/CultivationProgressService.js` 增加修为，达到 `1000 / 1000` 后不再继续累积。
- 服务会区分实际获得值与溢出值；满修为时明确提示需要突破。纯经验物品不会在满值时被消耗，普通清修
  到达瓶颈会立即停止并保留实际所得，秘籍本身仍可学会但附带修为只补到上限。
- 角色存档 schema 已升级到 v5。旧档中 `18961 / 1000` 一类超额修为在迁移后会规范为 `1000 / 1000`，
  迁移逻辑集中在 `core/save/SaveMigrations.js`，没有在 UI 的 `render()` 中临时修档。

### 38.2 突破不再是单按钮成功

- 满修为进入闭关室后，普通闭关入口会切换为“突破修为”。炼气突破已实现可实际操作的“灵息凝旋”：
  灵机经过金色气穴时按空格或点击中央“凝”字，五次评分达到门槛才成功；失败损失当前修为的 10%，
  成功才推进境界、把修为归零并保存。
- 突破事务由 `domain/cultivation/CultivationBreakthroughService.js` 结算，小游戏规则由
  `BreakthroughTrialService.js` 负责，`ui/sect/BreakthroughMinigamePanel.js` 只负责画面与输入。
- `BreakthroughTrialCatalog.js` 已为不同大境界固定不同玩法类型：炼气为灵息凝旋、筑基为地脉连阵、
  金丹为丹火控温、元婴为神魂符序、化神为穿越天劫裂隙。**目前只有炼气玩法完成可操作实现**；
  后四种是稳定目录与后续接口，不能对外描述成已经做完，也不能退回点击按钮直接突破。

### 38.3 第一章可玩的探索、选择与战斗循环

- 第一章已经从“接任务后直接取古玉”改为：**接村长委托 → 采集异光灵草 → 雾岚岔路选择 →
  稳妥绕行或挑战雾隐山魈 → 前往古潭取得古玉**。任务箭头、右侧目标和任务日志会随阶段同步。
- `domain/quests/ChapterQuestService.js` 使用
  `gather-herb / choose-path / defeat-guardian / find-ancient-jade / completed` 五个稳定阶段；场景只转发
  地图事件，不能靠重复按键或坐标越级完成。旧进行中档会显式补到第一步，旧完成档保持完成状态。
- 稳妥路线奖励 `灵石 × 4`；风险路线胜利奖励 `灵石 × 12` 和 `星萤果 × 1`。战败或离开后仍停在挑战
  阶段，可回岔路重试。奖励使用稳定怪物 ID 防重复发放，并通过现有战斗奖励服务结算。
- 已完成旧主线的角色可在任务日志中选择“重新体验”，通过 `restartChapter()` 一次性重置章节状态、引路、
  古玉、精英标记和必要的角色状态，不需要新建角色。

### 38.4 古玉选择与进入战斗的两处卡死修复

- 古玉剧情改为真实的 `立即迎战 / 暂时离开` 双选项，可点击、按数字键或用 E/空格确认第一项；Esc 可安全
  关闭。原先普通对话结束回调和不可见“返回地图”按钮造成的假卡死已经移除。
- 真正阻止战斗创建的问题是固定劫修没有编辑器模板时传入 `null`，而
  `core/MonsterAppearance.js` 直接访问 `null.appearance`。统一外观解析器现在对 `null`、旧空值和缺失模板
  安全回退；固定敌人使用默认魔蛛动画，自定义怪物继续使用上传立绘。
- 主角和魔蛛两套原有 153 帧动画完整保留，没有为降低加载量而抽帧。已在真实浏览器进入战斗画面验证，
  浏览器错误日志为空；新增 `scripts/smoke-monster-appearance.mjs` 防止空模板问题回归。

### 38.5 角色创建规则领域化（当日最后完成项）

- 新增纯 JavaScript `domain/character/CharacterCreationService.js`，统一负责八字以内姓名、立绘与性别同步、
  五行十点分配、并列最高灵根的固定判定、初始技能预览，以及确认时的主属性和攻击计算。
- `CharacterCreateScene` 现在只处理 Phaser 绘制、输入和反馈，不再直接写姓名、性别、立绘、灵根、主属性
  或初始攻击。重新进入创建页时，剩余点数会从当前灵根数据重新计算；确认规则通过后才调用统一存档并
  进入栖霞村。`GameState.js` 中原先散落的五行常量和最高灵根函数已移入角色创建领域。
- 架构检查新增防回退规则：角色创建场景必须使用 `CharacterCreationService`，并禁止重新直接修改上述字段。
  `scripts/smoke-character-creation-service.mjs` 覆盖姓名清理与截断、空名、非法立绘/性别、立绘与性别同步、
  单灵根十点、减点、非法属性/步长、并列判定、未分完禁止确认和攻击计算。该项已通过自动测试；本轮尚未
  做角色创建全流程浏览器截图验收。

### 38.6 当前验证基线

- 2026-08-28 收工前已重新运行 `npm test`：**1 项架构检查和 28 组冒烟测试全部通过**。
- 28 组依次覆盖：章节任务、NPC 互动、NPC 对话、资料导入导出、法宝、战斗快捷栏、存档档案、角色属性、
  角色创建、修为封顶、修为突破、商店、建筑模板、建筑碰撞、小地图探索、门派准入、页面恢复、炼丹、
  控火小游戏、秘籍闭关、普通清修、三境试炼、编辑器文件、地图内容、怪物外观、战斗、战斗奖励和存档迁移。
- 怪物外观修复曾单独对相关 JavaScript 执行 `node --check`，并在浏览器实际进入古玉劫修战斗，错误日志为空。
  换电脑后仍要重新运行完整测试；修改 JavaScript 后继续执行相关 `node --check` 和 `git diff --check`。

### 38.7 当前工作区与不可误操作内容

- 当前 `git status --short` 中仍有角色创建领域化的未提交改动：`package.json`、
  `scripts/check-architecture.mjs`、`src/core/GameState.js`、`src/scenes/CharacterCreateScene.js`，以及未跟踪的
  `scripts/smoke-character-creation-service.mjs`、`src/domain/character/CharacterCreationService.js`；
  `架构规范.md` 已补充角色创建边界，本文也已更新当天交接。换电脑必须复制这些文件，不能只依赖已有提交。
- 旧嵌套副本 `F:\\webGame\\webGame\\` 和 `webGame.7z` 的删除是此前逐文件核对后的清理结果，已记录在
  第 34 节；不要擅自恢复。其他未跟踪素材和项目数据仍按用户资料保护，禁止清理工作区。
- 控制台的物品、怪物、NPC、建筑、地图摆放和上传图片保存在 `data/editor/` 与
  `public/assets/images/editor/`，随整个项目目录迁移；角色档案仍在浏览器存储，换机前必须另行导出。

### 38.8 下次开发的推荐顺序

1. 新电脑先完整阅读 `AGENTS.md`、本文和 `架构规范.md`，运行 `git status --short` 与 `npm test`；
   保留全部未提交和未跟踪文件，通过 `启动游戏.bat` 或 `node server.cjs` 启动，禁止重置工作区。
2. 先在真实浏览器完成一次角色创建全流程验收：改名、切换立绘/性别、分配十点、确认进入地图，并检查
   新角色主属性、攻击、头像和存档；发现问题应修领域服务或装配层，不能把规则写回场景。
3. 再从“重新体验第一章”完整试玩稳妥与风险两条路线，验证采集、岔路、战败重试、奖励、防重复、古玉
   选择和进入战斗；若页面仍加载旧脚本，先 `Ctrl + F5` 强制刷新，不要用重复点击掩盖问题。
4. 让第一章奖励立即有用途：优先实现“回村交付 / 炼丹 / 兑换功法碎页”的可见选择，并复用现有物品、
   背包、奖励和任务领域服务；随后再配置采集、精英战、商人、救援等区域随机事件模板。
5. 第一章循环稳定后实现筑基“地脉连阵”，它必须是与炼气不同的可操作规则、独立领域测试和 UI；
   金丹、元婴、化神依次按目录类型继续，禁止把未实现的大境界标记为可用。
6. 怪物掉落稳定奖励 ID、乱星海正式底图、宗门任务/宝库/传送及其余秘籍仍是后续技术与内容任务；
   继续兼容旧数据，不能为赶进度删除玩家已有模板、素材或存档。

## 39. 2026-08-29 门派总览与炼丹房 Pixso 改版

### 39.1 天剑宗总览

- 门派总览已按 Pixso“改版 / 进入门派”画板换用用户提供的无字雕花素材；门内修士、宗门名牌、
  宗门事务、炼丹房、闭关室和返回按钮仍由普通数据与 Phaser 文字叠加，未把具体门派数据烘焙进图片。
- 炼丹房、闭关室入口的圆章和名称按最终标注落在横牌中心；宗门事务正文使用固定内容宽度和中文逐字换行，
  不再因空格把整段文字提前换到下一行。
- 总览素材位于 `public/assets/images/sects/overview/`，绘制集中在
  `ui/sect/SectOverviewPanel.js`；门派配置和入口逻辑仍保持原领域边界。

### 39.2 炼丹房主界面

- Pixso“改版”页的 `炼丹房` 1920×1080 画板（节点 `64:1`）已读取完整节点树；背景、三栏主面板、
  丹方卡、炉体、进度条、材料行和按钮严格使用画板坐标、尺寸与用户提供的 `c2～c20`、`ux.jpg` 素材。
- 原始素材已复制到 `public/assets/images/pixso/alchemy/`。`SectScene` 统一预加载这些资源及炼丹所需灵草图片；
  `AlchemyRoomPanel` 只负责显示、点击和反馈，未修改丹方学习、材料校验、扣料、成丹率、控火小游戏或存档规则。
- 左栏四张丹方卡保持约 168px 的纵向节奏；中央使用原始丹炉大图和双增益卡；右栏按画板排列丹方详情、
  成丹概率、两味材料卡和底部操作。遇到三味材料丹方时会在相同内容区内紧凑排列三行，不遮挡来源与按钮。
- 顶部继续复用 `PlayerTopToolbar`。进入炼丹房或闭关室时临时隐藏总览右上宗门名牌，返回总览后恢复；
  炼丹房右上按钮已按最终标注显示“返回门派”，点击后关闭炼丹房并回到天剑宗总览。
- 炼丹房标题和三栏主体当前相对 Pixso 原始坐标整体上移 20px；左右主面板底图已按装饰方向互换。丹方卡及右侧
  详情的品阶小牌统一使用用户补充的 `b7.png`，中央丹炉的大品阶牌仍使用原素材。
- 已在本地浏览器验证默认丹方、切换丹方、两味/三味材料布局和丹炉选择弹层；页面控制台无警告或错误。

## 40. 2026-08-30 战斗胜利弹窗 Pixso 改版

- 已读取 Pixso“改版 / 新战斗界面-弹窗”1920×1080 画板（节点 `69:170`），遮罩、主面板、标题牌、分隔线、
  主按钮及文字均使用画板原始坐标和尺寸。主面板为 `696×528`、标题牌为 `414×90`、按钮为 `329×76`、
  分隔线为 `518×23`，不再使用通用 `XianxiaDialog` 近似绘制胜利弹窗。
- 用户提供的 `b1～b4.png` 已按用途复制并改名到 `public/assets/images/battle/result-dialog/`：
  `result-panel.png`、`result-title-plaque.png`、`result-primary-button.png`、`result-divider.png`。原始素材目录未修改。
- 新增独立 `ui/battle/BattleResultDialog.js`，只负责弹窗绘制、按钮输入和淡入淡出；`BattleScene` 仍负责胜利来源、
  奖励领域服务调用、生命灵气恢复、目标场景和 Enter/Esc 流程。战败双选项暂时继续使用原通用弹窗，因为当前
  Pixso 画板只提供了胜利单按钮方案。
- 已用 1 点生命的临时测试敌人走通真实攻击、胜利结算、弹窗确认和返回青云山流程；临时入口已删除，浏览器
  控制台无警告或错误。最终仍需以 `npm test`、相关 JavaScript 语法检查和 `git diff --check` 为统一验证。

## 41. 2026-08-30 炼丹房选择丹炉 Pixso 改版

- 已读取 Pixso“改版 / 炼丹房-选择凡炉”1920×1080 画板（节点 `70:1`）。当前画板不是旧版三列小卡弹窗，
  而是 `1650×800` 横向雕花主面板：面板坐标约为 `(135, 140)`，五张炉卡均为 `280×401` 并排排列，
  炉体图为 `224×224`，右上有 `78×78` 关闭按钮，底部取消按钮为 `349×81`。
- 用户提供的 `z1～z5.png` 和五张丹炉图已按用途复制、改名到
  `public/assets/images/pixso/alchemy/furnace-picker/`。原始素材目录保持不变；项目内文件名使用
  `picker-panel.png`、`furnace-card.png`、`furnace-grade-badge.png`、`furnace-*.png` 等稳定语义名称。
- 新增独立 `ui/sect/FurnacePickerDialog.js`，统一持有弹窗绘制、五卡命中、悬停、关闭和取消；
  `AlchemyRoomPanel` 只负责把 `AlchemyService.listFurnaces()` 的普通数据传入，并在选择后调用
  `selectFurnace(furnaceId)`。丹炉加成、稳定 ID、合法性、当前选择和存档没有写进 UI，也没有修改领域规则。
- 五张炉卡的名称、品阶、成丹加成和额外成丹均动态来自炼丹目录/领域服务；Pixso 中第三、第四张演示品阶
  与当前领域目录不一致时，以真实领域数据“玄品 / 地品”为准，避免为了还原示例文字篡改游戏规则。
- 已在真实 1920×1080 浏览器中验证弹窗视觉、五炉完整显示、九阳神炉 `+20% / +50%` 选择回写、取消、
  右上关闭和 Esc 关闭；临时直达炼丹房入口已删除，浏览器控制台无警告或错误。

### 41.1 炼丹房中央丹炉卡（同日最新稿）

- 已重新读取 Pixso“改版 / 炼丹房”节点 `64:1` 的最新中央区。第 39.2 节记录的“大号通用丹炉 + 独立大品阶牌”
  已被当前稿废止：中央现在显示一张完整 `280×401` 丹炉卡，炉体图为 `224×224`，卡内同时包含右上竖向品阶牌、
  分隔线、成丹/额外加成和丹炉名称。旧通用炉图与重复品阶牌已从主界面绘制中移除。
- 新增 `ui/sect/FurnaceCardView.js` 作为纯视觉组件，炼丹房中央与丹炉选择弹窗共用同一套炉体纹理映射、品阶牌、
  强调色和卡片排版；`FurnacePickerDialog` 不再维护第二份重复卡片实现。当前炉体仍来自
  `AlchemyService.getSelectedFurnace()`，切换玄铁、灵火、玄冰、地心或九阳后，炉图、品阶、名称与加成会一起更新。
- 中央卡按 Pixso 基准坐标 `(822.411, 347.186)` 放置；两张增益卡更新到 `y=790.318`，主按钮更新到
  `y=887.744`，累计统计更新到 `y=964.190`。它们继续跟随炼丹房主体统一上移 20px 的容器，不影响顶部工具栏和
  “返回门派”按钮。
- 已在 1920×1080 实机预览中以当前“九阳神炉”状态核对：卡片显示天品、成丹 `+20%`、额外 `+50%`，下方两张
  增益卡同步，炉图没有拉伸或裁切，文字没有重叠。用于直达炼丹房的临时预览入口已删除。

## 42. 2026-08-30 炼丹统一结果界面 Pixso 改版

- 已读取 Pixso“改版 / 炼丹房-炼丹成功”1920×1080 画板（节点 `70:583`）及完整节点坐标。界面使用
  `830×196` 成功顶牌、`680×535` 中央结果圆盘、`118×117` 丹药图、三张 `234×61` 阶段数据卡、
  `229×62` 火候评级牌和 `420×76` 返回按钮；全部按画板原始坐标绘制，不再使用旧版程序圆环近似结果页。
- 用户提供的 `ba.jpg`、`x2～x7.png` 与 Pixso 导出资源的 SHA-256 和像素尺寸逐项一致。项目内已按用途复制、
  改名到 `public/assets/images/pixso/alchemy/refinement-success/`：`success-background.jpg`、
  `success-title-plaque.png`、`success-result-disc.png`、`success-pill.png`、`success-stage-card.png`、
  `success-rating-badge.png`、`success-return-button.png`；原始素材目录保持不变。
- 新增独立 `ui/sect/AlchemyResultPanel.js`，只消费已经结算完成的普通结果数据，动态显示丹药名称、数量、
  控火评分、温炉/融药/凝丹三阶段比例和火候评级。组件不计算成功率、不扣药、不发奖励、不改存档；真实奖励仍由
  `AlchemyService.completeRefinement()` 原子结算，控火评分仍来自 `AlchemyMinigameService`。
- 初版接入曾只让“非演练且真实炼制成功”进入新结果层，导致玩家通过“控火演练”看到的成功与失败仍是旧圆环。
  现已修正为四条路径统一使用新雕花结果层：真实炼制成功、真实炼制失败、演练成功、演练失败。真实成功显示丹药
  和数量；真实失败不伪造产物；演练成功明确不消耗、不产丹；演练失败显示凝丹提示与赤色失败状态。
- 点击“返回丹房”或按 Esc 只关闭结果层并刷新丹房，已经入库的丹药不会重复发放；演练结果也不会触发奖励。
- `SectScene` 仅增加成功页资源预加载，符合场景负责资源生命周期、UI 负责绘制输入、领域负责结算的依赖方向。
  Pixso 参考 HTML 已保存到 `design-reference/pixso-alchemy-success/70-583.html`，其下载资源位于成功素材目录下的
  `reference/`，仅供后续坐标复核，不参与游戏运行。
- 已在真实 1920×1080 Phaser 画布中核对真实成功示例，并按玩家截图数据分别复现演练成功
  “评分 68、29% / 79% / 84%、火候稳定”和演练失败“评分 0、17% / 0% / 0%、火候炸炉”。两种演练结果
  均已完全离开旧圆环界面；背景、顶牌、圆盘、阶段卡、评级牌和返回按钮无拉伸、裁切或越界，临时入口已删除。
- 本轮新增及接入 JavaScript 已通过 `node --check`；收工前最终一次 `npm test` 的架构检查和 28 组冒烟测试
  全部通过，`git diff --check` 也通过。换电脑接手后仍需重新运行这些统一验证。

## 43. 2026-08-30 炼丹控火小游戏 Pixso 改版

- 已读取 Pixso“改版 / 炼丹房-小游戏”1920×1080 画板（节点 `70:1250`）及全部 69 个节点。当前正式稿使用
  墨金雕花全屏背景、三张 `254×69` 阶段卡、左侧 `412×577` 药性稳定面板、中央 `560×633` 丹炉主视觉、
  右侧 `418×578` 阶段说明面板、`882×61` 温度轨和成套催火/凝丹/放弃按钮，不再使用旧版程序矩形、圆锅和
  纯色面板近似绘制。
- 用户提供的 `ba.jpg`、`c1～c13.png`、`c91.png` 已按用途复制并改名到
  `public/assets/images/pixso/alchemy/minigame/`。项目内使用 `background.jpg`、`phase-card.png`、
  `stability-panel.png`、`cauldron-stage.png`、`stage-panel.png`、`temperature-track.png`、
  `temperature-needle.png`、`heat-button.png` 等稳定语义文件名；原始素材目录保持不变。
- `ui/sect/AlchemyMinigamePanel.js` 已按画板原始坐标重构，并导出独立的素材预加载入口交由 `SectScene` 调用。
  三阶段卡会按当前阶段切换金/玉/灰状态；左侧评分环和三阶段命中比例、右侧倒计时与目标温区、温度针、目标带、
  催火状态和凝丹就绪状态仍随真实小游戏会话实时更新。
- 本次只修改控火小游戏表现、命中区域和即时提示。炉温变化、阶段推进、目标温区、倒计时、凝丹条件、评分、炸炉、
  成丹加成与奖励结算仍由 `domain/alchemy/AlchemyMinigameService.js` 和 `AlchemyService.js` 负责；没有把规则、扣料、
  发奖或存档写入 UI。
- Pixso 画板原始导出图已保存在 `design-reference/pixso-alchemy-minigame/70-1250.png`，后续调整应继续以该图和
  节点 `70:1250` 为准，不要凭截图比例重新猜坐标。已在真实 1920×1080 Phaser 画布中核对全部素材、文字和间距，
  并实际点击“放弃本炉”验证退出；浏览器控制台没有警告或错误，临时预览入口已删除。
- 按 2026-08-30 最终校正，右侧阶段说明面板中央的倒计时数字相对 Pixso 基准单独下移 `10px`，文字中心
  `y` 从 `493.327` 调整为 `503.327`；面板标题、操作说明、目标温区和输入提示位置保持不变。
- 门派总览、炼丹房和闭关室顶部的六个共用入口已从占位说明改为真实功能：储物袋、法术、功法、法宝和存档
  直接打开 `CharacterMenuPanel` 对应独立页签，设置直接打开完整游戏设置弹窗；关闭后仍停留在原门派页面。
  角色菜单显示素材由统一预加载入口登记，刷新后直接恢复炼丹房或闭关室也能完整显示，不依赖先经过大地图。

## 44. 2026-08-30 闭关室 Pixso 改版

- 已读取 Pixso“改版 / 闭关室”1920×1080 画板（节点 `70:1601`）的完整画面和节点坐标。当前正式稿使用洞府
  全屏背景、左右 `244×292 / 243×292` 法术与功法入口、中央 `500×500` 打坐人物、`640×167` 时长底板、
  四张 `108×47` 时长按钮、`350×134` 开始按钮、`276×81` 返回按钮和右上 `201×156` 闭关室名牌。
- 用户提供的 `ba.jpg`、`n1～n7.png` 已保留原始目录不动，并按用途复制、改名到
  `public/assets/images/pixso/retreat-room/`：`background.jpg`、`spell-entry.png`、`technique-entry.png`、
  `duration-option.png`、`start-retreat-button.png`、`return-sect-button.png`、`room-plaque.png`、
  `duration-panel.png`。本地素材未包含的打坐人物已从同一 Pixso 节点 `70:1839` 原尺寸导出为
  `meditating-cultivator.png`，没有使用旧角色图近似替代。
- `ui/sect/RetreatRoomPanel.js` 已导出 `preloadRetreatRoomAssets()`，由 `SectScene` 负责调用；主界面改用语义化
  新素材，旧 `pixso-retreat-meditation` 只继续服务原有秘籍参悟覆盖层。场景仍只负责资源和服务装配，UI 只负责
  绘制、命中与反馈。
- 主界面严格使用 Pixso 原始设计坐标：打坐人物左上 `(708, 311.5)`，法术入口中心 `(574, 598.25)`，功法入口
  中心 `(1343.73, 598.25)`，时长底板左上 `(630.5, 750.8)`，四档按钮中心横坐标依次为
  `777.51 / 900.51 / 1023.51 / 1146.52`，开始按钮左上 `(777.51, 951.86)`，返回按钮左上
  `(1596.25, 944.71)`。浏览器只做等比例缩放，不按窗口尺寸重新猜布局。
- 玩家姓名、境界、修为数值和百分比继续读取真实角色数据；1/10/30/50 年切换、逐步获得修为、提前出关、
  修为瓶颈、突破、法术/功法秘籍参悟与存档仍由原 `CultivationRetreatService`、`RetreatStudyService` 和突破服务
  负责。本轮没有把任何修为公式、奖励结算或存档写入 UI。
- 已在本地 Phaser 1920×1080 设计画布中核对洞府背景、人物、双入口、时长滑轨、四档按钮、开始按钮和返回按钮；
  实际点击法术与功法入口分别打开各自秘籍页，切换到 10 年后标题、奖励与滑轨同步更新，开始闭关可推进修为并在
  满值时切换突破状态，返回门派可恢复总览。浏览器控制台无错误，临时直达验收入口已删除。
- Pixso 原画板快照和坐标清单保存在 `design-reference/pixso-retreat-room/`。后续微调必须以节点 `70:1601`、
  `70-1601.png` 和该目录 `README.md` 为准，不要依据缩放截图再次估算。统一收工验证仍为 `npm test`、相关
  JavaScript `node --check` 和 `git diff --check`。
- 2026-08-30 最新视觉校正：角色名下方的修为进度条和闭关年限上方的时长滑轨均改为圆角轨道；内部填充也按
  当前进度实时重绘圆角端点，避免 0%～100% 变化时重新出现直角。滑块位置、数值计算和闭关规则不变。

## 45. 2026-08-30 闭关室参悟成功 Pixso 改版

- 已读取 Pixso“改版 / 闭关室-参悟成功”1920×1080 画板（节点 `70:1896`）及实际节点坐标。成功层使用
  50% 全屏黑色遮罩、位于 `(518, 131)` 的 `874×771` 墨金圆环、`170×170` 技能图、`190×181` 雕花外框、
  58px 成功标题、30px 领悟说明、`448×22` 分隔线和双侧继续菱形，不再使用旧版“单张技能图 + 两行文字”近似层。
- 用户提供的 `m1～m4.png` 已保留原始目录不动，并按用途复制、改名到
  `public/assets/images/pixso/retreat-room/study-success/`：`success-disc.png`、`skill-frame.png`、
  `divider.png`、`continue-diamond.png`。技能图仍读取真实参悟结果对应纹理，名称使用动态
  `result.study.name`，没有把“火球术”烘焙进界面素材或改动领域数据。
- 成功层由 `RetreatRoomPanel.openSuccessOverlay()` 负责绘制和输入；参悟校验、秘籍消耗、法术/功法写入、
  修为奖励和防重复结算仍由 `RetreatStudyService` 负责。旧版三秒自动消失已按当前效果稿改为“点击任意位置继续”，
  点击只关闭结果层并刷新闭关室，不会再次结算或重复发放奖励。
- Pixso 原画板快照与坐标表保存在 `design-reference/pixso-retreat-study-success/`。已用真实 Phaser
  1920×1080 画布核对圆环、技能框、标题、说明、分隔线和继续提示，并实际点击画面空白处返回闭关室；
  浏览器控制台无警告或错误，临时验收入口已删除。

## 46. 2026-08-30 当日完整交接与明日接手顺序

本节是 2026-08-30 当天工作的集中交接。第 39～45 节保留了各界面的详细坐标、素材和实现过程；换电脑后应先读
本节掌握全貌，再按需回看对应小节。今天的改动都遵守 `架构规范.md`：场景只做资源生命周期和装配，UI 只负责
绘制、命中和反馈，领域服务继续负责规则、结算和存档。仓库不设置行数禁令，是否拆分仍以职责边界为准。

### 46.1 今天完成的界面与交互

- **进入门派 / 门派总览**：按 Pixso 节点 `62:1` 和用户素材完成总览改版，校正炼丹房、闭关室、门内修士、
  宗门事务和顶部工具栏的尺寸、位置、文字换行与点击范围。宗门事务正文按面板实际宽度换行，不再出现尾部大段空位。
- **炼丹房主界面**：按节点 `64:1` 完成三栏布局、标题、丹方列表、炼制筹备、增益卡、按钮和返回门派入口；中央
  丹炉改为完整丹炉卡，不再使用通用大炉图和重复品阶牌。主界面、总览与闭关室使用同一套顶部入口交互。
- **更换丹炉**：按节点 `70:1` 新增独立 `FurnacePickerDialog`，五种丹炉完整展示，可通过卡片、关闭按钮、取消或
  Esc 操作；选择结果会写回真实炼丹服务并刷新中央丹炉卡。`FurnaceCardView` 负责主界面和弹窗共用的丹炉视觉。
- **炼丹结果**：按节点 `70:583` 新增 `AlchemyResultPanel`，真实成功、真实失败、演练成功、演练失败四条路径统一
  使用新界面；真实产物、数量、评分和阶段数据均来自结算结果，演练不会扣料、发丹或伪造奖励。
- **炼丹控火小游戏**：按节点 `70:1250` 重做全屏界面、三阶段卡、左侧稳定度、中央丹炉、右侧阶段说明、温度轨和
  操作按钮。最终校正为右侧倒计时相对 Pixso 基准下移 `10px`；控火算法、评分、炸炉和结算仍在领域服务中。
- **战斗胜利弹窗**：按节点 `69:170` 新增独立战斗结果弹窗组件并接入真实胜利流程，保留回青云山、键盘确认和
  生命/灵气恢复提示。当前只有胜利稿有完整 Pixso 设计，战斗失败仍保留旧界面，不能擅自套用胜利样式。
- **顶部六个功能入口**：门派总览、炼丹房和闭关室中的储物袋、法术、功法、法宝、存档、设置均已从占位提示改为
  真实跳转或弹窗；关闭后回到原门派页面。功法、法术、法宝、储物袋仍是独立页签和独立模块，没有互相借页面取数据。
- **闭关室主界面**：按节点 `70:1601` 完成洞府背景、打坐人物、法术/功法入口、闭关时长底板、1/10/30/50 年、
  开始闭关和返回门派按钮。角色修为条及闭关时长滑轨已改为圆角轨道和圆角动态填充。
- **参悟成功界面**：按节点 `70:1896` 完成墨金圆盘、动态技能图、雕花技能框、成功标题、真实技能名称和点击继续
  提示。旧三秒自动消失已改为点击任意位置关闭；关闭只刷新界面，不会重复消耗秘籍或重复结算奖励。

### 46.2 今天新增或集中维护的组件边界

- `src/ui/battle/BattleResultDialog.js`：只负责战斗结果弹窗的绘制、输入和回调；胜负判定、奖励和恢复规则不在组件内。
- `src/ui/sect/FurnacePickerDialog.js`：只负责五炉选择弹窗；丹炉目录、选择合法性和存档由 `AlchemyService` 负责。
- `src/ui/sect/FurnaceCardView.js`：炼丹房与选择弹窗共用的纯丹炉卡视觉，不持有领域状态。
- `src/ui/sect/AlchemyResultPanel.js`：只展示已经结算完成的炼丹结果，不计算成功率、不扣材料、不发奖励。
- `src/ui/sect/AlchemyMinigamePanel.js`：只绘制控火小游戏并转发输入；温炉、融药、凝丹、评分和结果由
  `AlchemyMinigameService` 负责。
- `src/ui/sect/RetreatRoomPanel.js`：承载闭关室主界面、参悟覆盖层和成功反馈；修为增长、秘籍消耗、领悟写入和存档
  继续由 `CultivationRetreatService`、`RetreatStudyService` 等领域服务负责。
- `src/scenes/SectScene.js` 与 `src/scenes/BattleScene.js` 只新增了素材预加载、组件装配和页面切换协调，没有把业务
  规则搬入场景。后续继续修改时不得反向把上述独立组件重新堆回场景或另一个页面类。

### 46.3 素材与 Pixso 参考索引

- 门派总览运行素材：`public/assets/images/sects/overview/`。
- 炼丹运行素材：`public/assets/images/pixso/alchemy/`，其中丹炉选择、炼丹结果和控火小游戏分别位于
  `furnace-picker/`、`refinement-success/`、`minigame/`。
- 战斗结果弹窗运行素材：`public/assets/images/battle/result-dialog/`；其他新战斗 UI 素材位于
  `public/assets/images/battle/new-ui/`。
- 闭关室运行素材：`public/assets/images/pixso/retreat-room/`；参悟成功素材位于其 `study-success/` 子目录。
- Pixso 离线参考与坐标资料：`design-reference/pixso-alchemy-success/`、
  `design-reference/pixso-alchemy-minigame/`、`design-reference/pixso-retreat-room/`、
  `design-reference/pixso-retreat-study-success/`。后续微调优先读取这些参考和对应节点，不要仅凭缩放截图猜坐标。
- 用户提供的 `D:/游戏素材/新UI/` 原始目录均未删除或覆盖；项目运行依赖的是已经复制、改成语义名称并登记预加载的
  项目内素材。换电脑时必须复制整个仓库，尤其不能遗漏 Git 尚未跟踪的图片、字体、组件和设计参考目录。

### 46.4 当前验证状态与已知未完成项

- 2026-08-30 收工前重新执行 `npm test`，架构检查和全部 28 组冒烟测试通过；当天新增 JavaScript 已执行
  `node --check`，工作区差异也执行过 `git diff --check`。在新电脑上仍须重新建立一次相同基线。
- 主要界面均在真实 1920×1080 Phaser 画布中做过视觉和点击验收，临时直达入口已删除；继续开发时不得把验收
  快捷入口留进正式流程。
- **战斗失败弹窗尚未改版**：目前没有对应 Pixso 效果稿和完整素材，继续保留旧实现，等用户提供设计后再独立接入。
- **闭关室秘籍内容仍按现有领域能力开放**：当前真正实现并可结算的参悟内容仍以领域服务返回的数据为准，不能只为
  填满 UI 私自在面板中新增法术、功法或存档字段。
- 当用户继续指出像素位置、间距、圆角或换行问题时，应在对应独立 UI 组件内做局部校正，并同步本说明；不要为了一处
  视觉调整改动领域规则或破坏其他页面的共用工具栏行为。

### 46.5 明天换电脑后的固定操作

1. 打开仓库后依次完整阅读 `AGENTS.md`、`项目开发说明.md`、`架构规范.md`；本节不是替代架构规范，两份文档必须
   同时遵守。
2. 运行 `git status --short`，确认今天留下的已修改和未跟踪文件；禁止 reset、checkout、清理或覆盖这些用户改动。
3. 运行 `npm test` 建立新电脑基线。若依赖未安装，只恢复项目原有依赖，不升级版本、不顺手重构无关模块。
4. 使用 `启动游戏.bat`，或在仓库根目录运行 `node server.cjs`，以 1920×1080 设计画布逐条回归：
   - 门派总览 → 炼丹房 → 更换丹炉 → 开始炼丹 → 控火小游戏 → 成功/失败结果 → 返回丹房；
   - 门派总览 → 闭关室 → 切换年限 → 法术/功法参悟 → 成功层 → 点击任意位置继续 → 返回门派；
   - 进入战斗并取得胜利 → 核对新胜利弹窗 → 返回青云山；
   - 在门派总览、炼丹房和闭关室分别点击顶部六个入口，确认跳转到对应独立界面并能正常返回。
5. 继续开发时先处理用户最新截图反馈；如果要新增战斗失败稿、闭关秘籍或其他玩法，先确认效果稿和领域数据是否已经
   存在，再按独立模块接入。每轮完成后执行 `npm test`、新增 JavaScript 的 `node --check` 和 `git diff --check`，
   并把新的验收结果追加到本文件最后一章。

## 47. 2026-08-31 新电脑接手基线与浏览器回归

### 47.1 接手基线

- 已按固定顺序完整阅读 `AGENTS.md`、本说明和 `架构规范.md`，之后才运行仓库检查；本轮没有执行
  `git reset`、`git checkout`、`git clean`，也没有删除、覆盖或还原已有成果。
- 接手时 `git status --short` 只显示 8 个既有的 `.codex-temp/prototype_refs/26～33` 设计参考图为未跟踪文件，
  没有已修改的源码文件；这些参考图保持原样，未处理。
- 接手基线 `npm test` 通过：架构检查和全部 28 组冒烟测试均通过。

### 47.2 真实浏览器回归结果

- 在项目当前 1920×1080 Phaser 设计画布中完成门派总览、炼丹房和闭关室回归；Codex 内置浏览器窗口会对设计
  画布等比显示，因此验收点击仍使用组件登记的 1920×1080 设计坐标，没有改动运行布局。
- 门派总览、炼丹房、闭关室中的储物袋、法术、功法、法宝、存档、设置六个顶部入口均逐一打开，能进入对应
  独立界面或设置弹窗，并能正常关闭后返回原门派页面。
- 炼丹房已验证主界面、五种丹炉选择弹窗、选择玄铁丹炉、回灵丹方参悟、控火演练、三阶段自动结算、演练失败
  结果层和“返回丹房”链路。演练失败层标题、阶段评分、返回按钮和背景遮罩显示正常。
- 闭关室已验证 1/10/30/50 年切换，法术秘籍和功法秘籍弹窗均读取现有领域服务数据。当前浏览器档位未持有
  《火球术要略》和《清心诀》等秘籍，领域服务正确阻止参悟并给出提示；本轮没有为了显示成功层伪造秘籍、法术、
  功法或存档字段。
- 通过附近修士资料卡的正式“战斗”按钮进入项目已有测试战斗，普通攻击两回合取得胜利；新战斗胜利弹窗的标题、
  敌人名称、测试战斗说明、生命/灵气恢复提示和“返回青云山”按钮均正常，点击后正确恢复青云山场景。
- 完整回归后检查浏览器控制台，`error` 和 `warn` 均为空。本轮未发现需要修改源码的确定性视觉或交互问题。

### 47.3 数据前置条件与下一步

- 当前浏览器档位的背包没有回灵丹所需药材，因此只能验证不会消耗材料的控火演练，不能完成真实“开炉炼丹”的
  成功或失败结算；后续获得真实药材后应补做这一条，不能直接修改背包数据绕过领域校验。
- 当前浏览器档位没有闭关参悟所需秘籍，因此不能在不修改存档的前提下进入“参悟成功”层；后续通过正式玩法获得
  秘籍后，再回归学习中、成功层、点击任意位置继续和返回门派完整链路。
- 验收过程中仅通过正常游戏交互在当前浏览器档位选中了玄铁丹炉并参悟回灵丹方；项目源码、编辑器 JSON、素材路径
  和既有存档结构均未修改。
- 战斗失败弹窗仍按第 46 节约束保留旧界面，等待用户提供独立 Pixso 效果稿后再处理。下一轮优先响应用户提供的最新
  截图与像素位置反馈，不自行改动已经确认的门派、炼丹、闭关和战斗胜利界面。

## 48. 2026-08-31 闭关室法术 / 功法秘籍选择界面改版

### 48.1 本轮完成内容

- 按 Pixso 页面“改版”节点 `73:243` 重做闭关室“法术秘籍”选择层。主面板、标题、分隔线、关闭按钮、六张
  秘籍卡、秘籍图标、品阶属性、参悟条件和“已选”挂签均按 1920×1080 设计坐标实现。
- 新增 `src/ui/sect/RetreatBookPickerDialog.js` 作为法术与功法共用的纯 UI 组件；组件只负责绘制、输入和回调，
  不读取领域服务、不消耗秘籍、不修改存档。
- `RetreatRoomPanel` 继续负责把 `getRetreatBookPreviews()` 与 `RetreatStudyService` 返回的普通数据装配给选择层；
  点击后的合法性判断、学习计时、秘籍消耗与参悟结算仍由原领域服务完成，没有把规则写回 UI。
- “功法秘籍”使用同一套效果图和尺寸。当前领域数据只有《清心诀》，所以仅显示这一项，剩余五个卡位显示
  “尚未收录”；没有为填满界面新增功法、法术、存档字段或伪造可学习内容。

### 48.2 素材与设计参考

- 用户原始素材目录 `F:/游戏素材/素材/新UI/闭关/法术秘籍/` 保持不变；`l1～l5` 已复制、改为语义名称并放入
  `public/assets/images/pixso/retreat-room/book-picker/`。同一 Pixso 节点中的标题分隔线和境界小图标也已导出到
  该目录，运行时不依赖用户盘符之外的路径。
- Pixso 原节点的离线 HTML、效果图、素材映射和精确坐标表保存在
  `design-reference/pixso-retreat-book-picker/`。后续微调应先读取该目录，不要从缩放截图反推位置。
- 卡片内容以领域数据为准，因此《九霄神雷诀》继续显示项目真实的“仙品 · 雷系”，不照抄效果稿中的临时错字。

### 48.3 验证与后续约束

- 已在真实浏览器中从门派总览进入闭关室，分别打开法术和功法秘籍选择层；法术六项、功法一项加五个空卡位、
  “已选”挂签和关闭按钮均显示、点击正常，浏览器控制台只有 Phaser 启动信息，没有警告或错误。
- 本轮修改后的 `RetreatBookPickerDialog.js` 与 `RetreatRoomPanel.js` 已执行 `node --check`；`npm test` 的架构检查
  和全部 28 组冒烟测试通过，`git diff --check` 通过。
- 后续新增秘籍只能先扩展现有领域模板与纯 JavaScript 测试，再由此组件展示；禁止直接在选择层硬编码可学习规则，
  也禁止让法术与功法模块互相借页面方法读取数据。

## 49. 2026-08-31 闭关室学习中界面 Pixso 改版

### 49.1 本轮完成内容

- 按 Pixso 页面“改版”节点 `73:482` 重做闭关室“学习中”覆盖层；当前 Pixso 节点使用墨金引气光环方案，
  与用户附带的较早蓝色圆环截图不同，因此实现以当前节点为准。
- 新增 `src/ui/sect/RetreatLearningOverlay.js` 作为独立纯 UI 组件，按 1920×1080 原始坐标绘制 50% 黑色遮罩、
  `840×576` 引气光环、`500×500` 打坐人物、58px 金色标题、`807×67` 进度轨道、动态渐变填充、滑标、
  30px 进度文字和底部说明。
- `RetreatRoomPanel` 只把领域服务返回的初始进度和后续进度交给新组件，并继续保留原计时器装配；开始学习、推进、
  完成、秘籍消耗、奖励与存档仍由 `RetreatStudyService` 负责。本轮没有新增或修改任何玩法规则和存档字段。

### 49.2 素材与设计参考

- 用户原始目录 `F:/游戏素材/素材/新UI/闭关/学习中/` 未删除、未覆盖；`l6～l9` 已复制并改为语义名称，放入
  `public/assets/images/pixso/retreat-room/learning/`。其中当前节点实际使用进度轨道、进度滑标和引气光环，
  `section-divider.png` 作为本次完整素材归档保留。
- 打坐人物继续复用闭关室已有 `meditating-cultivator.png`，避免重复引入相同角色资源。新素材由闭关室面板统一
  预加载，运行时不依赖用户素材盘符。
- Pixso 节点截图、离线 HTML、节点原始素材和精确坐标表保存在
  `design-reference/pixso-retreat-learning/`；后续像素微调应以节点 `73:482` 和该目录 `README.md` 为准。

### 49.3 浏览器回归与验证

- 已在真实 1920×1080 Phaser 游戏流程中打开功法秘籍，选择当前档位可学习的《清心诀》，成功进入新学习中界面。
  黑色遮罩、引气光环、打坐人物、标题、轨道、滑标、进度文字和说明均正常显示；随后原领域计时正常完成并进入
  已有“参悟成功”层，证明学习中 UI 没有截断完成和结果流程。
- 真实回归同时确认法术 / 功法秘籍选择层仍可正常切换、关闭和反馈；浏览器控制台 `warn`、`error` 均为空。
- 本轮相关 JavaScript 已执行 `node --check`；`npm test` 的架构检查和全部 28 组冒烟测试通过，
  `git diff --check` 通过。
- 后续若调整学习时长、秘籍条件或结算结果，必须在领域服务和纯 JavaScript 测试中修改；
  `RetreatLearningOverlay` 只能接收并显示普通数据，禁止在其中加入秘籍消耗、奖励或存档逻辑。

### 49.4 最新截图视觉校正

- 根据实机截图反馈修正进度条渐变方向。旧实现受 Phaser Graphics 渐变顶点顺序影响，实际画面变成“左金右绿”；
  现已反转绘制端点，恢复 Pixso 中的“左绿 `#277B3C` → 右金 `#E5A800`”，进度值和滑标规则不变。
- “引气入体...”继续使用 Pixso 指定的阿里妈妈东方大楷，但改为逐字绘制，并加入克制的字号、基线、角度、横纵比例、
  金色深浅和墨影差异，消除整串文字完全同高同角度的电脑排字感；标题整体中心与原效果稿坐标不变。
- 已在真实浏览器中重新走通“门派总览 → 闭关室 → 功法秘籍 → 《清心诀》 → 学习中 → 参悟成功”；逐字标题显示正常，
  学习计时、完成层和点击流程未受影响，浏览器警告与错误日志为空。校正后再次执行相关 JavaScript 语法检查、
  `npm test` 和 `git diff --check`，结果全部通过。

## 50. 2026-08-31 游戏设置界面 Pixso 改版

### 50.1 本轮完成内容

- 读取 Pixso“改版”页面当前设置画板，并使用用户提供的成套宣纸墨金素材重做游戏设置界面。新版采用浅色宣纸大框、
  墨金边框、青玉云纹、悬挂流苏、独立标题牌和菱形关闭按钮，不再使用旧版深棕通用弹窗。
- 新增 `src/ui/settings/GameSettingsDialog.js` 作为设置专用纯 UI 组件；组件只负责素材预加载、1920×1080 坐标绘制、
  按钮命中、关闭动画和反馈文字。全屏切换、资料导入导出、保存当前进度及返回封面继续调用场景已有流程和 `core`
  服务，组件没有实现存档规则或数据迁移。
- `VillageScene` 与 `SectScene` 均改为装配同一个 `GameSettingsDialog`。六个按钮按当前效果依次使用深色、深色、金色、
  深色、深色、红色底图；标题、副标题、按钮文字和关闭图标均按素材中心坐标对齐。旧深色弹窗使用的浅色状态反馈已
  在组件内转换为适合宣纸背景的深绿、深红、赭金和灰褐色，避免导入取消、导出成功等提示看不清。

### 50.2 素材与设计参考

- 用户原始目录 `F:/游戏素材/素材/新UI/设置/` 保持不变；`m1、m2、m3、m5、m8、m11` 已复制并改成语义名称，
  放入 `public/assets/images/pixso/settings/`。运行时不依赖用户素材盘符，也没有删除或覆盖原文件。
- 素材尺寸、运行坐标和文件映射记录在 `design-reference/pixso-game-settings/README.md`。主面板使用原始
  `1115×688` 尺寸，三类按钮分别保持 `411×68`、`411×69`、`411×64`，关闭图标保持 `70×72`。
- 本轮 Pixso 桌面画板可正常查看，但本地 MCP 端点暂时未监听，因此参考文档没有伪造节点 ID；连接恢复后若继续做
  像素微调，应先补登记当前画板节点，再以节点原始坐标复核。

### 50.3 浏览器回归与验证

- 已在真实浏览器的 1920×1080 Phaser 画布中，从门派总览顶部设置入口打开新版界面，核对主面板、标题牌、副标题、
  六个按钮、金色按钮文字对比度、底部反馈位置和关闭图标；关闭图标、红色关闭按钮与 Esc 均沿用原有关闭流程。
- 已验证窗口化操作仍调用原场景方法并在新宣纸面板底部显示状态反馈；没有触发导入文件选择或改写用户本地资料。
- `GameSettingsDialog.js`、`VillageScene.js`、`SectScene.js` 已执行 `node --check`；`npm test` 的架构检查和全部
  28 组冒烟测试通过，`git diff --check` 通过。

### 50.4 设置按钮悬浮状态校正

- 根据最新实机截图反馈，红框内五个功能按钮统一使用深色默认状态；鼠标进入任一按钮命中区域时才切换为金色高亮
  素材，鼠标移出后恢复深色。底部“关闭”继续保持红色，只沿用原有轻微悬浮反馈；村庄和门派场景共用同一设置组件，
  全部功能回调保持不变。

## 51. 2026-08-31 角色档案界面 Pixso 改版

### 51.1 本轮完成内容

- 按 Pixso“改版”页面当前 `角色档案` 画板（节点 `78:1`）重做五档位角色选择页。新版使用墨金标题牌、整块花纹
  总面板、五张独立档案卡、圆形山水头像框、空档位加号徽记、绿/红功能按钮和底部返回牌，全部按 1920×1080
  设计坐标实现。
- 新增 `src/ui/save/CharacterArchiveView.js` 作为角色档案专用纯 UI 组件；组件只负责素材预加载、绘制、圆形头像裁切、
  悬浮反馈和点击回调。`SaveSlotScene` 只读取 `GameState` 提供的档位普通数据并装配进入、创建、删除和返回流程，
  没有把存档规则或迁移逻辑写进 UI。
- 页面保持项目既有的最多 5 个档位、已有角色继续游戏、空档位进入创建页、删除前确认和返回封面规则；没有修改
  存档结构、角色数据或玩法字段，也没有在回归中点击删除现有角色。

### 51.2 素材与设计参考

- 用户原始目录 `F:/游戏素材/素材/新UI/角色/角色档案/` 未删除、未覆盖；九张原始素材已复制并改为语义名称，放入
  `public/assets/images/pixso/character-archive/`，运行时不依赖用户素材盘符。
- 素材原始尺寸、运行坐标、文件映射和职责边界记录在 `design-reference/pixso-character-archive/README.md`。
- 已在 Pixso 桌面端图层树确认当前画板节点 `78:1`；本地 Pixso MCP 端点当时未监听，因此没有伪造 D2C 数据，
  本轮以当前可见画板、1920×1080 画布和用户原始 PNG 尺寸完成像素落地。

### 51.3 浏览器回归与验证

- 已在真实浏览器中从游戏设置执行“保存并退出到封面”，再经“踏入仙途”进入新版角色档案；已有角色头像、名字、
  境界和进度显示正常，四个空档位的创建入口与五卡间距正常，浏览器控制台只有 Phaser 启动信息，无警告或错误。
- 已验证空档位“创建角色”能进入既有角色创建页，创建页“返回角色档案”能返回，新版底部“返回封面”能回到封面，
  已有角色“进入游戏”能重新进入青云山；上述回归没有创建新角色或删除、覆盖现有档位。
- `CharacterArchiveView.js` 与 `SaveSlotScene.js` 已执行 `node --check`；`npm test` 的架构检查和全部 28 组冒烟测试通过，
  `git diff --check` 通过。后续如修改档位数量、创建条件、删除规则或存档迁移，必须继续在 `core/domain` 对应边界处理，
  角色档案 UI 只能接收普通数据并转发用户操作。

### 51.4 首轮实机截图校正

- 根据用户标注将顶部“角色档案”和底部“返回封面”的背景素材对调；首版把这两张外形相近的墨金牌匾用途识别反了，
  现已按实机反馈恢复正确位置，并同步使用各自素材的原始尺寸。
- 档案位编号改用清晰的微软雅黑粗体，字号由 17px 调整为 19px，移除浅色描边并提高深色文字对比度，解决宣纸标签上
  “档案位 1～5”发虚、不易辨认的问题。
- 已有角色的进度文字和“进入游戏”按钮分别向上移动 10px；角色姓名、境界、删除按钮及空档位布局保持不变，避免
  无关区域跟随移动。

### 51.5 第二轮实机截图校正

- 三个已有角色档位的“删除角色”按钮统一向上移动 5px，按钮文字随素材整体移动，其他卡片内容和两个空档位保持不变。
- “返回封面”文字不再按包含底部流苏透明区的整张 PNG 画布中心定位，改为按牌匾主体的实际可见区域居中；牌匾本身
  仍保持页面水平中心和原始尺寸，底部说明位置不变。

### 51.6 第三轮实机截图校正

- 根据五张卡片与总面板左右边距的标注，将五个档案位整体统一向左移动 10px；卡片背景、编号、头像、角色资料、
  进入/删除按钮以及空档位内容均随所属卡片一起移动，卡片之间的 39px 可见间距保持不变。
- 总面板、顶部“角色档案”、底部“返回封面”和说明文字继续保持页面中心，不随卡片行移动。

## 52. 2026-08-31 全界面刷新位置恢复

### 52.1 本轮完成内容

- 扩展 `src/core/SceneResumeState.js` 的会话级页面恢复记录。浏览器刷新后不再一律回到青云山大地图，而是先恢复刷新前
  所在的稳定界面；记录只保存在当前标签页的 `sessionStorage`，不会新增存档字段，也不会污染另一档角色。
- 当前已覆盖封面、角色档案、角色创建、头像选择、青云山、章节结果、战斗、门派总览、炼丹房、闭关室，以及青云山和
  门派中的属性、储物袋、法宝、法术、功法、社交、存档、设置界面。关闭界面时会把恢复目标同步退回所属场景，避免
  之后刷新重新打开已经关闭的面板。
- `CharacterMenuPanel` 只通过普通回调上报稳定的界面 ID，场景负责装配，恢复记录和档位校验全部留在 `core`；没有把
  存档或业务规则写入 UI。角色创建页补齐自身背景和头像预加载，因此直接刷新进入创建页或头像选择层时也不会出现
  缺失纹理。
- 炼丹控火、闭关学习中、奖励结算等可能产生消耗或奖励的过程界面不保存半完成事务；它们继续按现有领域服务的安全
  恢复策略回到对应功能入口，避免刷新导致重复扣除、重复奖励或重复结算。

### 52.2 测试与真实浏览器回归

- 扩充 `scripts/smoke-scene-resume-state.mjs`，覆盖封面设置、角色档案、角色创建与头像选择、青云山功能页、档位隔离、
  非法场景拒绝、门派功能页和门派内功能面板；同时保留战斗、损坏记录保护等既有检查。
- 已在真实浏览器中逐项刷新验证：角色档案、角色创建、头像选择、青云山储物袋、青云山法术、青云山设置、门派总览、
  炼丹房、炼丹房内储物袋和炼丹房内设置均停留原界面；背景、头像和功能素材加载正常，控制台无运行错误。
- 本轮修改的 JavaScript 已执行 `node --check`；`npm test` 的架构检查和全部冒烟测试通过，`git diff --check` 通过。

### 52.3 后续接手注意事项

- 新增可独立进入的稳定页面时，应先在 `SceneResumeState` 增加受控白名单和纯 JavaScript 测试，再由场景记录/恢复；
  禁止把任意场景名或未经校验的数据直接写入恢复记录。
- 涉及交易、炼丹、闭关、战斗奖励等可改变存档的中间步骤，只有在领域服务具备幂等事务恢复设计后才允许恢复到处理中
  状态；在此之前必须恢复到安全父页面，不能仅为视觉连续性跳过结算安全边界。
