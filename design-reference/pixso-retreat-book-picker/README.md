# 闭关室秘籍选择界面参考

- Pixso 页面：`改版`
- 目标节点：`73:243`（`闭关室-法术`）
- 设计画布：`1920 × 1080`
- 离线原始参考：`73-243.html`
- 离线效果图：`73-243.png`

## 运行素材映射

用户提供的 `F:/游戏素材/素材/新UI/闭关/法术秘籍/` 原素材保持不变，项目只复制并改成语义名称：

- `l1.png` → `book-picker-panel.png`（原图 `1094 × 933`）
- `l2.png` → `book-card.png`（原图 `442 × 225`）
- `l3.png` → `book-card-selected.png`（原图 `442 × 225`）
- `l4.png` → `selected-ribbon.png`（原图 `42 × 90`）
- `l5.png` → `close-button.png`（原图 `72 × 72`）

标题分隔线与境界小图标来自同一 Pixso 节点的导出资源。运行素材统一放在
`public/assets/images/pixso/retreat-room/book-picker/`。

## 关键坐标

- 主面板中心：`(960, 540)`，显示尺寸 `1094 × 933`
- 标题中心：`(960, 130.5)`，字号 `49px`，颜色 `#DDAC4F`
- 标题分隔线中心：`(960, 194.545654)`，显示尺寸 `463 × 27`
- 关闭按钮中心：`(1436.226685, 140.5)`，显示尺寸 `72 × 72`
- 六张卡片中心：
  - `(713.264282, 328.688232)`
  - `(1185.505249, 328.688232)`
  - `(713.264282, 573.688232)`
  - `(1193.960938, 569.733887)`
  - `(713.264282, 818.688232)`
  - `(1185.505249, 818.688232)`
- 卡片尺寸：`442 × 225`
- 秘籍图标：`120 × 120`，相对卡片中心偏移 `(-101.25, 3)`
- 已选挂签：`42 × 90`，相对卡片中心偏移 `(-162.26416, -45.27832)`

## 实现说明

- Phaser 实现位于 `src/ui/sect/RetreatBookPickerDialog.js`，法术与功法共用同一纯 UI 组件。
- 字体优先使用 `SJ yuantijian-C-Regular`，不存在时回退到微软雅黑。
- 卡片名称、品阶、属性和参悟条件读取现有领域服务数据；Pixso 文案只作视觉参考。
- 功法当前只有领域服务已有的《清心诀》，其余卡位显示“尚未收录”，不新增玩法或存档字段。
